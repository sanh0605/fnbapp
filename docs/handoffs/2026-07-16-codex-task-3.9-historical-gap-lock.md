# Task: Task 3.9 — Backdated-Ledger Historical Gap Cohort Lock

## Context

Task 3.8 (`ad7f7ba`) confirmed: 41 BACKDATED_LEDGER_LIKE outside-cohort lines map to 5 historical `Stock_Ledger` rows, but **0 durable `backdated_ledger_events`** (migration 0014 is "after insert" only, no historical backfill). Admin UI cannot surface these for operator review.

User chose Option A: accept as drift. Lock 41 lines as historical-gap cohort. Same policy as Task 3.7 (`BTP_RECIPE_REPLAY_DRIFT`).

**Financial impact**: none. Stored COGS at sale time was what the operator recorded; drift is replay-only. No recompute.

## Scope

### Cohort composition (41 lines)

| Source | Lines | Unique cohort delta |
|---|---:|---:|
| Task 3.4 BACKDATED_LEDGER_LIKE (all) | 41 | -43,809 VND |

**Per-line breakdown** — load from `docs/audits/2026-07-16-task-3.8-backdated-events-surface.json`. Each line has:
- `line_id`, `order_id`, `sale_time`
- `stored_cost_at_sale`, `expected_cost_at_sale`, `delta_vnd`
- `historical_causal_ledger_rows[]` (1-3 ledger row IDs per line)
- `durable_coverage`: `HISTORICAL_GAP` for all 41

Note: per-ledger-row deltas overlap (22 lines map to multiple rows). Do NOT sum per-row deltas — use the unique 41-line cohort delta from the JSON.

### Explicitly excluded

- **130 E3 cohort lines** (already locked, do not touch).
- **225 Task 3.7 BTP_RECIPE_REPLAY_DRIFT lines** (already locked, do not touch).
- **40 E3 PURCHASE_COST_RECOVERY lines** (subset of 170 baseline locks, already reconciled).

## In scope

1. Author `scripts/lock-backdated-historical-gap-cohort.ts`:
   - Read-only by default (`--apply` gate for production write).
   - Pure planner + tests pattern (mirror `lib/btp-drift-lock.ts` + `lib/btp-drift-lock.test.ts` from Task 3.7).
   - Input: `docs/audits/2026-07-16-task-3.8-backdated-events-surface.json` (41 line records).
   - Compute source hash (SHA-256 of canonicalized JSON of 41 records, sorted by line_id, fixed field order).
   - Validate each line:
     - Still exists in `Order_Lines_V2`.
     - Not already in `audit_baseline_locks` (idempotent check, 0 overlap with 395 existing).
     - `cost_at_sale` matches Task 3.8 JSON value.
   - Insert rows into `audit_baseline_locks` with:
     - `order_line_id`: from Task 3.8 JSON.
     - `locked_by`: `"Codex Task 3.9"`.
     - `locked_at`: now (DB default).
     - `reason`: `"BACKDATED_LEDGER_HISTORICAL_GAP — migration 0014 historical backfill gap, no durable event, see Task 3.8 report 2026-07-16"`.
     - `source_hash`: computed SHA-256.
     - `stored_cost_at_sale`: current stored value (post-validation).
     - `expected_cost_at_sale`: current replay value (post-validation).
     - `delta_vnd`: `expected - stored` (per audit sign convention).

2. Dry-run output must include:
   - Total lines to lock (41).
   - Computed source hash.
   - Per-line validation result (pass/fail with reason).
   - Overlap check with existing 395 locks (expect 0).
   - Expected post-apply total: 395 + 41 = **436**.

3. Apply path (`--apply`):
   - Atomic single transaction.
   - Post-insert verification: count = 436, trigger blocks sample UPDATE.

4. Documentation:
   - New result doc `docs/audits/2026-07-16-task-3.9-lock-result.md` (before/after/dry-run/atomic/idempotent sections — mirror Task 3.7 result doc structure).
   - Append `DEVELOPMENT-TRACKING.md` entry.

## Out of scope

- Do NOT recompute any `cost_at_sale` value.
- Do NOT backfill `backdated_ledger_events` for the 5 historical ledger rows.
- Do NOT modify migration 0014 trigger logic.
- Do NOT modify admin UI.
- Do NOT modify MAC engine code.
- Do NOT push to remote.

## Constraints

- **Dry-run default**: no `--apply` flag → read-only.
- **Idempotent**: re-running on already-locked lines must be safe no-op (skip + report).
- **Atomic**: single transaction for the entire cohort insert.
- **Verification gates** (all must pass):
  - Count: `audit_baseline_locks` row count = 395 existing + 41 new = **436 total**.
  - Cohort: 41 rows with reason `BACKDATED_LEDGER_HISTORICAL_GAP`.
  - Trigger probe: sample UPDATE on a locked line must fail with "audit-baseline locked".
  - Sign: inserted deltas match source JSON deltas within 1 VND rounding.
  - No `cost_at_sale` value changed by this task.
- **No push** — local commit only.

## Reusable existing code (Task 3.7 pattern)

- `lib/btp-drift-lock.ts` — planner structure, validation logic, BUCKET_POLICY pattern. Clone + adapt.
- `lib/btp-drift-lock.test.ts` + `lib/btp-drift-lock-script.test.ts` — test pattern.
- `scripts/lock-btp-recipe-replay-drift-cohort.ts` — CLI script pattern (dry-run + apply).
- `supabase/migrations/0012_mac_drift_baseline_locks.sql` — existing schema, no new migration needed.

## Verification

- Script runs cleanly with `vite-node scripts/lock-backdated-historical-gap-cohort.ts` (dry-run).
- `--apply` path: completes atomically, no partial inserts.
- Post-apply SQL check: `SELECT count(*), reason FROM audit_baseline_locks GROUP BY reason` shows new cohort.
- `vitest run`: 365/365 pass baseline (from Task 3.8) + new planner tests.
- `tsc --noEmit`: 0 errors.
- `git diff --check`: clean.

## Expected output

- `lib/backdated-historical-gap-lock.ts` (new planner + constants).
- `lib/backdated-historical-gap-lock.test.ts` (new tests).
- `lib/backdated-historical-gap-lock-script.test.ts` (new CLI tests).
- `scripts/lock-backdated-historical-gap-cohort.ts` (new CLI script).
- `docs/audits/2026-07-16-task-3.9-lock-result.md` (new result doc).
- `DEVELOPMENT-TRACKING.md` entry dated 2026-07-16.
- Commit: `Codex lock: Task 3.9 backdated historical gap cohort (41 lines)`.
- No push.

## Stop-and-ping triggers

Stop and ping Claude before continuing if:

- Any of the 41 line IDs is missing from `Order_Lines_V2` (line was deleted; skip or abort decision).
- Any source-JSON `cost_at_sale` value differs from current DB value by >1 VND (line was edited; current vs source decision).
- Any of the 41 IDs overlaps with existing 395 locks (classification bug; abort).
- Lock insert fails partway (do NOT silently retry; report state).

## Priority

P1 — final cleanup before stabilization phase (UI audit + Drive backup + push). Codex pickup expected. Same model tier as Task 3.7 (`gpt-5.6-sol` High — production write requires careful reasoning).

## After Task 3.9

MAC drift audit will be **fully clean**:
- 170 E3 baseline locks (40 recovered + 130 intentionally retained).
- 225 Task 3.7 BTP_RECIPE_REPLAY_DRIFT.
- 41 Task 3.9 BACKDATED_LEDGER_HISTORICAL_GAP.
- **Total: 436 locked lines. Zero unexplained live mismatches.**

Then stabilization phase (UI audit + Drive backup + push) can proceed without MAC drift distractions.

## Questions before starting

- Should the script also write a brief policy addendum to `docs/audits/2026-07-16-btp-recipe-replay-drift-policy.md` extending it to cover BACKDATED_LEDGER_HISTORICAL_GAP? Or keep Task 3.9 as standalone lock with reference to Task 3.8 report?
