# Task: Task 3.7 — BTP Recipe Replay Drift Cohort Lock

## Context

Task 3.6 closed (`d32d4d4`). Root cause: temporal asymmetry — order line pins top-level recipe but BTP shortfall decomposition uses current nested BTP recipe at replay. Stored COGS correct at sale time; drift is replay-only artifact. No financial impact.

User chose Option B (Accept + lock). Policy doc: `docs/audits/2026-07-16-btp-recipe-replay-drift-policy.md`.

This task: insert 225 lines into `audit_baseline_locks` as the `BTP_RECIPE_REPLAY_DRIFT` cohort. No recompute. No migration (existing 0012 supports this).

## Scope

### Cohort composition (225 lines total)

| Source | Bucket | Lines | Delta | Reason |
|---|---|---:|---:|---|
| Task 3.4 outside-cohort | PRE_BASELINE_WINDOW | 90 | -107,225 VND | BTP recipe replay drift (historical) |
| Task 3.4 outside-cohort | BASELINE_SELECTION_GAP | 22 | -25,662 VND | BTP recipe replay drift (in-window, missed by frozen snapshot) |
| Task 3.6 post-cutoff frozen | POST_CUTOFF_NEW_DRIFT | 71 | -67,221 VND | BTP recipe replay drift (active) |
| Task 3.6 newer lines | LATE_PO_RECEIPT (durable) | 42 | +6,809 VND | Migration 0014 backdating (expected, captured) |
| **Total** | | **225** | **-193,299 VND** | |

### Explicitly excluded

- **41 BACKDATED_LEDGER_LIKE** (Task 3.4): remain on Task 3.2 admin UI review path. **Do NOT lock** in this task — they need operator/accounting review first.
- **130 locked cohort lines** from E3 (34 BACKDATED_LEDGER + 96 UNRESOLVED): already locked, do not touch.
- **40 recovered PURCHASE_COST_RECOVERY lines**: already reconciled in E3.

## In scope

1. Author `scripts/lock-btp-recipe-replay-drift-cohort.ts`:
   - Read-only by default (`--apply` gate for production write).
   - Inputs:
     - `docs/audits/2026-07-15-task-3.4-outside-cohort-investigation.json` (112 lines: 90 PRE_BASELINE + 22 BASELINE_SELECTION_GAP).
     - `docs/audits/2026-07-15-task-3.6-forward-drift-investigation.json` (113 lines: 71 frozen + 42 newer).
   - Compute combined source hash (SHA-256 of canonicalized JSON of the 225 line records).
   - Validate each line:
     - Still exists in `Order_Lines_V2`.
     - Not already in `audit_baseline_locks` (idempotent check).
     - `cost_at_sale` matches the value recorded in the source JSON.
   - Insert rows into `audit_baseline_locks` with:
     - `order_line_id`: from source.
     - `locked_by`: `"Codex Task 3.7"`.
     - `locked_at`: now (DB default).
     - `reason`: `"BTP_RECIPE_REPLAY_DRIFT — temporal asymmetry, stored COGS correct at sale time, see policy doc 2026-07-16"`.
     - `source_hash`: computed SHA-256.
     - `stored_cost_at_sale`: current stored value (post-verification).
     - `expected_cost_at_sale`: current replay value (post-verification).
     - `delta_vnd`: `expected - stored` (per policy sign convention).

2. Dry-run output must include:
   - Total lines to lock.
   - Per-bucket breakdown.
   - Any lines that failed validation (with reason).
   - Computed source hash.

3. Apply path (`--apply`):
   - Atomic insert (single transaction).
   - Post-insert verification: count rows, verify trigger blocks sample UPDATE.

4. Documentation:
   - Update `docs/audits/2026-07-16-btp-recipe-replay-drift-policy.md` "Cohort lock implementation" section with post-apply row count + verification result.
   - Append `DEVELOPMENT-TRACKING.md` entry.

## Out of scope

- Do NOT recompute any `cost_at_sale` value.
- Do NOT touch the 41 BACKDATED_LEDGER_LIKE lines (Task 3.2 path).
- Do NOT touch the 130 already-locked lines.
- Do NOT modify migration 0012 (existing schema sufficient).
- Do NOT modify MAC engine code.
- Do NOT modify the audit script (`lib/mac-cogs-audit.ts`) — Task 3.5 owns any audit-cohort changes.
- Do NOT push to remote.

## Constraints

- **Dry-run default**: no `--apply` flag → read-only.
- **Idempotent**: re-running on already-locked lines must be safe no-op (skip + report).
- **Atomic**: single transaction for the entire cohort insert.
- **Verification gates** (all must pass before claiming complete):
  - Count: `audit_baseline_locks` row count = 170 baseline (includes 40 E3-recovered) + 225 drift = **395 total**.
  - Trigger probe: sample UPDATE on a locked line must fail with "audit-baseline locked".
  - Sign: inserted deltas match source JSON deltas within 1 VND rounding.
  - No `cost_at_sale` value changed by this task.
- **No push** — local commit only.

## Risks

1. **Cohort boundary drift**: ensure 90 PRE_BASELINE + 22 SELECTION_GAP from Task 3.4 are distinct from 71 frozen + 42 newer from Task 3.6. Verify 0 ID overlap between groups.
2. **Idempotency**: if script is re-run after partial failure, must not double-insert. Use `ON CONFLICT (order_line_id) DO NOTHING` or pre-check.
3. **Trigger interaction**: lines already in `audit_baseline_locks` from E3 cannot be re-locked. Skip + report.
4. **Lock window**: short transaction; ~225 row insert should be <1 second. No contention expected.

## Verification

- Script runs cleanly with `vite-node scripts/lock-btp-recipe-replay-drift-cohort.ts` (dry-run).
- `--apply` path: completes atomically, no partial inserts.
- Post-apply SQL check: `SELECT count(*), reason FROM audit_baseline_locks GROUP BY reason` shows new cohort.
- `vitest run`: 353/353 pass (no test files modified).
- `tsc --noEmit`: 0 errors.
- `git diff --check`: clean.

## Expected output

- `scripts/lock-btp-recipe-replay-drift-cohort.ts` (new, dry-run + apply).
- `docs/audits/2026-07-16-btp-recipe-replay-drift-policy.md` (update "Cohort lock implementation" section with results).
- `docs/audits/2026-07-16-task-3.7-lock-result.md` (new, before/after table + verification log).
- `DEVELOPMENT-TRACKING.md` entry dated 2026-07-16.
- Commit: `Codex lock: Task 3.7 BTP recipe replay drift cohort (225 lines)`.
- No push.

## Stop-and-ping triggers

Stop and ping Claude before continuing if:

- Any of the 225 line IDs is missing from `Order_Lines_V2` (line was deleted; need to decide whether to skip or abort).
- Any source-JSON `cost_at_sale` value differs from current DB value by more than 1 VND (line was edited; need to decide whether to use current or skip).
- Any of the 225 IDs overlaps with the existing 170 baseline locks or the 41 BACKDATED_LEDGER_LIKE bucket (classification bug; abort).
- Lock insert fails partway (do NOT silently retry; report state).

## Priority

P1 — Task 3.7 in `docs/ROADMAP.md`. Codex pickup expected. Same model tier as Task 3.6 (`gpt-5.6-sol` High — production write requires careful reasoning).

## Questions before starting

- Should the script also handle a future "extend cohort" path (e.g., next BTP recipe edit creates a new batch), or is this a one-shot for the current 225?
- For the 42 LATE_PO_RECEIPT lines (positive delta), is the policy the same — accept as expected backdating behavior, lock as `LATE_PO_RECEIPT` cohort (separate from `BTP_RECIPE_REPLAY_DRIFT`)?
- For the 7 ambiguous-recipe lines (Task 3.6 noted schema lacks `Recipes.recorded_at`), should we lock them under `BTP_RECIPE_REPLAY_DRIFT` anyway, or flag separately?
