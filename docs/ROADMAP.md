# Roadmap — Pending Work

Single source of truth for pending tasks, priorities, and scope boundaries. Updated when tasks are added, started, completed (move to `COMPLETED.md`), or blocked.

## How to use this file

- **Start of session:** read this file to know what to work on
- **Add new task:** append to appropriate priority queue with owner + scope
- **Start task:** mark status, mention in commit body
- **Complete task:** move entry to `COMPLETED.md`, remove from here
- **Block task:** move to "Blocked" section with reason

## Active agents & scope

| Agent | Role | Owns | Reviews |
|---|---|---|---|
| Claude (GLM 5.1) | Coordinator | `docs/**/*.md`, root `*.md`, deploy, tracking | All cross-scope changes |
| Codex (GPT 5.5) | Engine (unavailable, see note) | `lib/*.ts`, `supabase/migrations/*.sql`, `scripts/*.ts` | Engine/data correctness |
| Claude Sonnet 5 | **Sole active implementer (owner decision 2026-07-27)** | Everything: `app/**`, `components/**`, `lib/*.ts`, `supabase/migrations/*.sql`, `scripts/*.ts` | Self-review (temporary, see note) |
| Antigravity (Gemini 3.1) | Backup only (2026-07-24) | — | Independent visual QA when asked |

**2026-07-27 note:** Codex hit its usage limit again mid-session (this time while planning `PERF-2` Phase B execution, after re-confirming approach 1 with the owner) and the owner declared it done for the foreseeable future — "chỉ còn mỗi em là agent còn lại cho dự án này." Sonnet 5 now covers `lib/*.ts`, `supabase/migrations/*.sql`, and `scripts/*.ts` directly, including self-review, superseding the earlier time-boxed exceptions (`REV-2`–`REV-5`). Revert this table if Codex becomes available again — no standing decision has been made to permanently retire the review-split model, only to stop blocking on an agent that isn't answering.

Detailed scope rules: `docs/COLLABORATION.md` section C (Risk-Boundary Ownership).

## Commit protocol for parallel work

**When 2+ agents active simultaneously, Claude serializes commits:**

1. Agent A works → commits → signals Claude
2. Claude reviews diff → approves or requests changes
3. Claude signals Agent B to start
4. Repeat

**Never run concurrent commits.** Past incidents: bundled commits required `git reset --hard` + cherry-pick to split.

**Cross-scope exception (small fixes):** <10 lines, 1 file, explicit Claude approval. Document in commit body.

## Priority queue

### P0 — In progress now

| Task | Owner | Scope | Started | Notes |
|---|---|---|---|---|
| (none) | — | — | — | `REBUILD-1` (full-history inventory + COGS ground-truth rebuild) closed 2026-07-22 — see `docs/COMPLETED.md`. |

### P1 — Next up (high impact, unblocked)

| Task | Owner | Scope | Notes |
|---|---|---|---|
| [x] **DEP-1. Dependency vulnerability triage (found 2026-07-24, first-ever `npm audit` run)** | Codex | `package.json`, `package-lock.json` | **Closed 2026-07-24 (commits `6b6fdad`/`a8b0d03`), reviewed by Claude Sonnet 5 2026-07-25.** Phase 1 (Sonnet, `1459e61`): `next-auth` chain fixed. Phase 2 (Codex): `vitest` 1.6.1→4.1.10 (closes the critical vitest-UI advisory; needed `vitest.config.ts` oxc/jsx config + `vite-node` added as an explicit devDependency after a follow-up regression), `googleapis` moved to devDependencies (confirmed zero runtime imports first). Audit: 21→12 findings, 0 critical. Full writeup: `docs/audits/2026-07-24-dependency-vulnerability-triage.md`. Verified independently: `tsc` clean, 744+/744+ tests, build passed. Remaining `next`/`postcss` chain correctly deferred to `INFRA-UPGRADE-1` (needs React 19 + full framework migration, not a `--force` bump). |
| [x] **PERF-2. Wave 3 performance remediation (owner-approved 2026-07-24)** | Codex | `app/pos/page.tsx`, `app/admin/activity-log/**`, PERF-1 pages, `app/admin/orders/actions.ts` ledger bounds | **Phase A + most of Phase B done 2026-07-24 (commits `17533d3`/`14d45fc`/`60e8757`/`5fb76c3`), reviewed by Claude Sonnet 5 2026-07-25.** POS dead out-of-stock computation removed (was hardcoded to an empty array already — confirmed the whole `Stock_Ledger`+`Recipes` fetch feeding it was pure waste); activity-log server-paginated at the database with injection-safe search (verified: test explicitly sends `(`, `)`, `,` in the search term and confirms they're stripped before building the PostgREST `.or()` filter string); `PERF-1`'s `history.replaceState` fix landed as an opt-in `sync: "history"` option; void/edit `Stock_Ledger` reads now scoped by `reference_id`/`item_reference` instead of full-table fetches (verified the item-reference collection correctly expands nested semi-product recipes, so the bound doesn't miss implicit-production rows). **Phase B implemented 2026-07-27 by Claude Sonnet 5** (Codex re-confirmed approach 1 with the owner and started planning execution, then hit its usage limit again mid-session before writing any code — owner declared Sonnet 5 the project's sole remaining agent and asked it to execute Codex's own already-written design/plan directly). Migration `0038_materialize_inventory_balances.sql` (commit `18bafdd`): new `inventory_balances` table kept in sync by an `AFTER INSERT OR DELETE OR UPDATE` trigger on `stock_ledger`, covering every write path (atomic RPCs, void/edit reversal, stocktake, full-history rebuild) in the same transaction; privileged `rebuild_inventory_balances()` for manual recovery only. Read-only drift audit (commit `cec3ab7`): `lib/inventory-balance-audit.ts` + `scripts/audit-inventory-balances.ts`. `getRealtimeStock`/`getPOSStockStatus` switched to read the balance table (commit `87997f3`) — deliberately kept the `sheets-Stock_Ledger` cache tag rather than the plan's suggested `sheets-Inventory_Balances`, since nothing in the app ever calls `revalidateTag` for the new table name (it's only ever written by the trigger). Owner ran `supabase db push` for `0038`, then code was pushed (`d013f84`). **Production incident found immediately after**: `inventory_balances` had no `id` column (used `item_reference` as its primary key instead), but every table in this schema is read through `lib/sheets_db.ts`'s hardcoded `.order("id")` pagination — broke `getRealtimeStock`/`getPOSStockStatus` in production for every request. Fixed within minutes via migration `0039` (commit `9a02a3f`, adds `id` = `item_reference` as the real primary key, keeps `item_reference` unique for the trigger's `ON CONFLICT`); owner ran `supabase db push` again. **Closed and verified live 2026-07-27**: `audit-inventory-balances.ts` 0 mismatches (54 items), `audit-current-stock.ts` same 3 known negative items as before (no new ones), `audit-pnl-mac-consistency.ts` 0 VND delta across 1,720 orders. `tsc` clean, full suite 759/759, build passed. |
| [x] **WF-1. Per-item purchase history + PO item-name search + supplier→PO links (owner-approved 2026-07-24)** | Claude Sonnet 5 | `app/admin/inventory/items/**`, `app/admin/inventory/purchase-orders/**`, `app/admin/suppliers/**` | Done 2026-07-24, commit `dd91596`. From `docs/audits/2026-07-24-workflow-forms-popups-search-audit.md` section D — both owner scenarios verified impossible before this. Read-only, no schema change. `tsc` clean, 678/678 tests, build passed. Not yet browser-verified (no login session available in this environment). |
| [~X] **INV-COUNT-1. Periodic guided stocktake (owner-approved 2026-07-24)** | S1 done by Claude Sonnet 5; **S2 + migration `0036` review now owned by Codex end to end** (owner decision 2026-07-24: engine-critical write paths and `supabase/migrations/*.sql` stay with Codex, not split with Sonnet) | New `stocktake_sessions`/`stocktake_lines` tables (done), `apply_stocktake_session_atomic` RPC writing STOCK_ADJUST (not started) | **Phase S1 done 2026-07-24, commit `88774a0`** — counting workflow (session open/save-line/cancel), migration `0036_stocktake_sessions.sql` **written but NOT yet applied to production**. **Codex's first task on pickup: review `0036` line-by-line** (it was written under the original plan's Sonnet-drafts/top-tier-reviews split; the owner has since tightened this so migrations stay fully in Codex's hands — rewrite it if Codex would rather not build on it, no production usage yet so no cost either way). **Then S2**: handoff ready, `docs/handoffs/2026-07-24-codex-inv-count-1-s2-confirm-and-apply.md` — new `apply_stocktake_session_atomic` RPC (models `approve_stock_adjustment_atomic` from migration `0019`), dry-run + confirm UI, writes `STOCK_ADJUST` rows only for nonzero-variance counted lines. Both migrations (`0036` + S2's new one) go through `supabase db push` together once reviewed. Directly attacks the root cause of every historical negative-stock incident — sequence this near the top of Codex's queue, after `DEP-1`'s in-progress vitest upgrade finishes, before or alongside `PERF-2`. |
| [x] **RPT-DIGEST-1. Daily summary page (owner-approved 2026-07-24)** | Claude Sonnet 5, coordinator review pending | `/admin/reports/daily`, read-only, reuses existing report actions | Phase D1 done 2026-07-24, commit `10af43c`. Plan: same doc, feature 2. `tsc` clean, 719/719 tests, build passed. Phase D2 (scheduled push) deferred pending owner channel choice + `CRON_SECRET`. |
| [x] **FC-1 (UI piece). POS split/mixed payment UI** | Antigravity (built 2026-07-20), Claude Sonnet 5 (reviewed + fixed 2026-07-24) | `components/POSScreen.tsx`, `components/pos/CartPanel.tsx` | Was actually already built and committed by Antigravity 2026-07-20 (`d631b10`) — the roadmap's pending-prompts entry was just never updated, so it kept reading "ready for Antigravity pickup" for 4 days. First real review of it (this session) found and fixed 2 bugs: all 3 checkout-retry paths dropped the `payments` array, silently turning a retried split payment into a full-amount single CASH payment; the client-side total wasn't rounded to match the server's `net_total`, which could make split payment impossible whenever a PERCENT order discount applied. Commit `dd4cada`. |
| [x] **UI-CLEAN-1. Design-free frontend cleanup sweep (owner-triggered frontend audit 2026-07-24)** | Claude Sonnet 5 | 14 files token swap, 9 dead form files deleted, 2/3 "Unknown" strings, 3 selects converted | Done 2026-07-24, commits `27b99aa`/`198c035`/`22df610`/`38b8d0f`. From `docs/audits/2026-07-24-frontend-ui-ux-audit.md` (FE-3/FE-5/FE-7). One deliberate gap: `app/admin/orders/actions.ts`'s 4 "Unknown" strings left for Codex (owns that file, was actively mid-edit on an unrelated task during this session). `tsc` clean, 694/694 tests, build passed. |
| [ ] **FC-3. Shift and cash reconciliation** | Claude (logic, covering Codex), Antigravity (functional-only UI) | New `shifts`/`cash_movements` tables, POS mandatory open-shift gate | Feature-completeness pass, item 3 of 3, deliberately last — no current staff makes this a non-issue for now. Plan: section 3 of the same roadmap doc. Owner confirmed shift-open is mandatory before selling, whenever this item's turn comes. |
| [x] **POS-OFFLINE-1. POS offline resilience (surfaced mid-session 2026-07-27 while brainstorming `ARCH-1`)** | Claude Sonnet 5 | `components/POSScreen.tsx`, `lib/pos-offline-queue.ts`, `app/admin/pos-sync/**`, `supabase/migrations/0040_*.sql`, `public/pos-sw.js` | **Fully closed 2026-07-28** (9/9 tasks + final whole-branch review, 2 fix rounds — all local commits, not pushed; 16 commits `de2fa25`..`4b73ac4`, see `DEVELOPMENT-TRACKING.md` for the full list). Spec: `docs/superpowers/specs/2026-07-27-pos-offline-resilience-design.md`. Plan: `docs/superpowers/plans/2026-07-27-pos-offline-resilience.md`. Client-captured sale timestamp, local IndexedDB order queue with background sync sweep (so a mid-sale network drop queues instead of blocking/rolling back), `synced_at`/`pos_sync_failures` migration + admin attention page for non-network sync rejections, and a minimal `/pos`-scoped service worker so the page can open with zero network at all (cache-first static assets, network-first-with-cache-fallback document). Task 6 needed a plan correction (commit `85eac43`) after the implementer correctly caught the original SQL targeting a function migration `0035` had already replaced with a payment-validating wrapper. **Final whole-branch review found 3 Critical + 2 Important cross-task defects invisible to per-task review** (idempotency fingerprint silently defeated by the new timestamp field, a queued order deletable on a failed sync-failure report, an admin page full-table scan, a service-worker redirect-caching risk, and a second-round regression the first fix round itself introduced) — all fixed and independently re-verified against the actual diffs, not the fix reports. Final verification: `tsc` clean, **803/803 tests**, build passed. **Migration `0040` written but NOT yet applied to production** — same pattern as `0038`/`0039`, owner's call via `supabase db push`; `app/admin/pos-sync` shows empty/error state until then. |

### P2 — Backlog (medium impact, functional bugs found during Pre-Audit C, not security exposures)

| Task | Owner | Scope | Notes |
| [x] **UI-CLEAN-1-FOLLOWUP. Last 4 "Unknown" -> "Không rõ" strings** | Codex | `app/admin/orders/actions.ts` (product/size snapshot fallbacks) | **Already closed** — Codex fixed all 4 occurrences in commit `8bb9d8a` (2026-07-24 evening batch, reviewed by Claude Sonnet 5 2026-07-25). Confirmed 2026-07-27: zero "Unknown" strings remain anywhere in `app/`. This row was stale, not an open item. |
|---|---|---|---|
| [x] **AUDIT-TOOL-1. `scripts/audit-order-ledger.ts` needs retirement or a rebuild on the new engine** | Codex | `lib/order-ledger-audit.ts`, `lib/full-history-ledger-audit.ts`, `scripts/audit-order-ledger.ts` | **Closed 2026-07-24 (commit `d370f6a`), reviewed by Claude Sonnet 5 2026-07-25.** Rebuilt on `lib/full-history-recompute.ts`'s ground truth: replays every eligible order chronologically, compares net derived quantity by order+item against the recorded ledger, 0.01-base-unit tolerance. Old `auditOrderLedger` kept only for older correction-verification scripts, explicitly deprecated for new audits. Live rerun found exactly 6 real mismatches (all 3 orders — see `COGS-5`/void-repair below), not the old tool's 3,585 stale false positives. Independently re-ran live 2026-07-25: **0 mismatches** across 1721 orders (the 6 are now resolved — see below). |
| [x] **COGS-5-FOLLOWUP. Pipeline lifecycle gap root-caused; original "second-event" premise disproved** | Codex | `app/api/cron/apply-backdated-corrections/route.ts`, `scripts/apply-pending-backdated-events.ts`, `lib/cogs5-pipeline-audit.ts` | **Closed 2026-07-24 (commit `a73287d`), reviewed by Claude Sonnet 5 2026-07-25.** `COGS-5`'s open question ("did a second-event coverage gap in `findAffectedLines` cause 41 lines to get only a partial correction?") was investigated with a new read-only audit joining the 85 COGS-5 events to all `data_recovery_changes` — found the 41 lines were never touched by the backdated-event pipeline at all; they were the `task-3.9-historical-gap-recovery-2026-07-21` cohort, deliberately preserved behind `audit_baseline_locks`, so a naive recompute disagreeing with them was expected, not a bug. Full writeup: `docs/audits/2026-07-24-cogs5-pipeline-root-cause-review.md`. The **actual** defect found: a zero-change backdated event was never marked `RECOMPUTED`, so it retried forever and left misleading lifecycle history (not a cost-correctness bug — `cost_at_sale` was never wrong). Fixed in both the cron route and the CLI apply script. Verified: 0 production writes during the investigation, `tsc` clean, full suite green. See `COGS-5`'s original entry in `docs/COMPLETED.md`-equivalent history for the underlying correction context. |
| [x] **VOID-EDIT-IMPLICIT-PRODUCTION. Void/edit didn't reverse implicit-production ledger rows** | Codex (void path) + Claude Sonnet 5 (edit path, 2026-07-25) | `lib/void-order-reversal.ts`, `app/admin/orders/actions.ts` (`voidOrderV2`/`editOrderV2`) | Found by Codex 2026-07-24 via the rebuilt `AUDIT-TOOL-1`: voiding an order that triggered implicit production (semi-product shortfall auto-covered from raw ingredients) only reversed the `SALES_CONSUME` row, permanently losing the `PRODUCTION_CONSUME` raw-ingredient deduction and double-counting the `PRODUCTION_YIELD` semi-product gain. Fixed for void in commit `4f6ba40` (new `lib/void-order-reversal.ts`, well-tested). **While reviewing that fix 2026-07-25, Claude Sonnet 5 found `editOrderV2` had the identical pre-existing gap** (confirmed via git history it predates this session) — fixed in commit `025abea` by reusing the same `buildVoidReversalRows` helper. Historical impact: exactly 3 production orders affected (`PHD001128`, `PHD001129`, `PHD001132`, 6 quantity mismatches), all via the void path — dry-run repair prepared in `scripts/repair-void-shortfall-ledger.ts` (commit `b309170`, gated `--apply`) but **live audit re-run 2026-07-25 already shows 0 mismatches** in production, so the repair appears already applied (not by a commit — direct data write, no git trace either way) or otherwise resolved; confirm with Codex when it's back rather than re-running `--apply` blind. |
| [x] **PERF-1. Some admin pages sync filters to the URL via Next navigation even though their data never depends on searchParams** | Codex | `app/admin/inventory/stock-adjustments/page.tsx`, `app/admin/promotions/page.tsx` | Found 2026-07-20, folded into `PERF-2`, closed 2026-07-24. `history.replaceState` fix landed via `lib/use-filter-form.ts`'s new `sync: "history"` option, confirmed (2026-07-25) used by both `StockAdjustmentsClient.tsx` and `PromotionsClient.tsx`. |
| [x] **REV-2. Independent re-review of Claude's scripts/ fixes made during Codex's rate-limit window** | Claude Sonnet 5, self-reviewed 2026-07-27 (Codex not returning, owner decision) | `scripts/audit-admin-action-auth.ts` + `-core.ts`/`-core.test.ts`, `scripts/audit-po-save-ledger.ts` + `-core.ts`/`-core.test.ts` | **Closed 2026-07-27.** Both fixes re-verified with fresh eyes rather than trusting the original commit claims: re-read current code, re-ran both audit scripts live (0 findings each: 102/102 guarded actions+routes, 0/58 PO ledger mismatches, up from 55), reran both `-core.test.ts` suites (12 + 6 passing). Found corroborating evidence `audit-admin-action-auth.ts` was implicitly load-bearing-tested already: Codex itself extended the exact `getRoutePolicy`/`classifyRouteStatus` functions 4 days later (commit `cf4c336`, adding a `SCHEDULED_SECRET` tier for the cron route) without flagging any issue with the `AUTHENTICATED` tier this review fix added. |
| [x] **REV-3. Independent re-review of Claude's FC-1 split-payment backend** | Claude Sonnet 5, self-reviewed 2026-07-27 (Codex not returning, owner decision) | `supabase/migrations/0024_pos_split_payment.sql`, `lib/order-cart.ts`, `lib/order-edit-cart.ts`, `lib/pos-order-transaction.ts`, `app/pos/actions.ts`, `app/admin/reports/actions.ts` | **Closed 2026-07-27.** Re-checked migration `0024`'s payment-sum validation: exact `bigint` equality (`v_payment_sum <> v_net_total`), no floating-point fragility, plus a post-insert row-count check preventing a silent partial write. Client-side (`lib/order-cart.ts`) enforces the identical sum/positive-amount invariant before the order is ever built, matching the server as defense-in-depth. `RPT-SALES` payment breakdown (`app/admin/reports/actions.ts`) correctly attributes revenue per payment line and dedupes order-count per method so a split payment doesn't double-count. Found corroborating evidence of a real prior review: Codex's own commit `4ff17c6` ("preserve split payments across order edits") built `planEditedOrderPayments` directly on top of this work 4 days later, fixing a genuine gap (editing an order with a preserved split payment) -- re-verified that fix too: rejects a changed total on an existing split rather than guessing a reallocation, 3 dedicated tests, correctly wired into `editOrderV2`. Reran all 5 directly relevant test files (48/48 passing) and both live production audits (order-ledger: 0/1744 mismatches; P&L/MAC: 0 VND delta). |
| [x] **REV-4. Codex retroactive review of Claude's FC-2 reorder-suggestion backend** | Codex — **addressed 2026-07-24, commits `68c7512`/`3ec2d7d`, reviewed by Claude Sonnet 5 2026-07-25** | `lib/reorder-suggestion.ts`, `lib/reorder-suggestion.test.ts`, `app/admin/inventory/actions.ts` | Codex is rate-limited until 2026-07-25; owner authorized Claude to cover Codex's logic/backend role for the feature-completeness pass's `REQUIRED_NOW` items, same pattern as `REV-3`. Design: `docs/superpowers/plans/2026-07-20-feature-completeness-required-now-roadmap.md` section 2. `computeReorderSuggestions` is a pure function (data fetching lives in the new `getReorderSuggestions()` action) that: sums `SALES_CONSUME`+`PRODUCTION_CONSUME` over a 14-day lookback for avg daily consumption; derives lead time from completed `Purchase_Orders`' `created_at` vs. the matching `PO_RECEIPT` `Stock_Ledger` row's `created_at` (falls back to 3 days when no history exists for that item); reorder point = avg daily consumption x lead time x 1.3 safety buffer; suggested reorder qty = 10-day target coverage x avg daily consumption, minus current stock, floored at 0, converted to purchase unit via `UOM_Conversions` when available. Items with fewer than 3 consumption events in the lookback window report `hasSufficientData: false` instead of a number. 9 new unit tests, `tsc --noEmit` clean, full suite 650/650 (up from 641), `next build` passed. Read-only throughout — no new write path, no schema change, no production data touched. **Also note for review**: the UI (`components/ReorderSuggestionTable.tsx`, wired into `app/admin/reports/stock/page.tsx`) was originally handed off to Antigravity (`docs/handoffs/2026-07-22-antigravity-fc2-reorder-suggestion-ui.md`, now superseded) but the owner asked Claude to build it directly instead given the task's simplicity — it did not get Antigravity's independent UI review, so Codex's pass should sanity-check it too, not just the backend. **Codex's own pass 2026-07-24 found and fixed 2 real bugs** in the logic it was reviewing: (1) a PO with multiple lines for the same item counted that PO's lead-time sample multiple times, skewing the average; (2) an invalid/zero/negative `conversion_rate` could produce `Infinity`/`NaN`/negative "suggested reorder quantity" instead of falling back to no-suggestion, and the purchase-unit quantity was truncated instead of rounded up (can't buy a fraction of a purchase unit). Commit `3ec2d7d` closed a related gap Codex found on its own: when an item has 2+ *active* unit conversions with no explicit preferred one, the old code silently picked whichever happened to be first instead of refusing to guess. Claude Sonnet 5 independently verified both commits' test coverage (explicit test cases for lead-time dedup and the ambiguous-conversion case) and reran the full suite. UI (`ReorderSuggestionTable.tsx`) sanity-check not separately re-verified this pass. |
| [x] **REV-5. Independent re-review of Claude Sonnet 5's coverage during Codex's second 2026-07-24/25 rate-limit window** | Claude Sonnet 5, self-reviewed 2026-07-27 (Codex not returning, owner decision) | `app/admin/orders/actions.ts` (`editOrderV2` reversal fix), `lib/history-ops/*` (3 files moved) | **Closed 2026-07-27.** (1) `editOrderV2`'s reversal fix (`025abea`) re-checked with a specific adversarial hypothesis: could editing the same order twice cause `buildVoidReversalRows` to double-reverse ledger rows already cancelled by the first edit? Traced the actual architecture and disproved it -- every edit creates a brand-new `Orders_V2` row (`id: \`ord-${crypto.randomUUID()}\``, supersede-and-replace, confirmed in `lib/order-edit-cart.ts`), so each order version's `Stock_Ledger` rows are isolated under their own unique `reference_id`; no cross-version overlap is possible. Reran `lib/void-order-reversal.test.ts` + `app/admin/orders/actions.test.ts` (7/7) and the live full-history ledger audit (0/1744 mismatches). (2) RS-2 batch 1 re-verified: grep confirmed zero stale import references to the 3 moved files, their 3 test suites all pass (14/14). |
| **PERF-1. Some admin pages sync filters to the URL via Next navigation even though their data never depends on searchParams** | Codex | `app/admin/inventory/stock-adjustments/page.tsx`, `app/admin/promotions/page.tsx` | Found 2026-07-20 (see `DEVELOPMENT-TRACKING.md`). `app/admin/orders/page.tsx` left this list 2026-07-24 (rebuilt with real server-side pagination). Remainder **folded into `PERF-2`** (Phase A3 of `docs/handoffs/2026-07-24-codex-wave3-performance-remediation.md`): swap URL sync to `window.history.replaceState`, no server-side pagination for these small tables. |
| **COGS-1-FOLLOWUP. CRON_SECRET** | Owner (env var) | Vercel project settings | COGS-1 itself is done (see `DEVELOPMENT-TRACKING.md` 2026-07-20 entries) -- automatic detection + correction now exists for both PO_RECEIPT and recipe-version backdating, with an anomaly-flagging safety net surfaced on the admin dashboard. One loose end: **owner must add `CRON_SECRET` to Vercel's environment variables** for `app/api/cron/apply-backdated-corrections` to actually run on its daily schedule -- cannot be set from here. (The 23 unflagged seed-era orders found during the investigation are now corrected -- see `DEVELOPMENT-TRACKING.md` 2026-07-21 entry and `docs/operations/backdated-cost-events-playbook.md`.) |
| **H1. Push local commits** | Claude | git | Khi anh yêu cầu. 41+ commits local pending. |
| **ARCH-1. Multi-outlet future-proofing design (DESIGN ONLY — owner trigger 2026-07-24)** | Codex (`gpt-5.6-sol` High), Claude review, owner approval | Design doc in `docs/superpowers/specs/` — no code, no migration | Owner identified the real architectural gap: the system's operating dimension is **brand**, but the physical unit is the **outlet/POS point** — a 3rd location under an existing brand cannot be represented today (`/pos?brandId=...`, `orders_v2.brand_id`, no outlet entity, `Stock_Ledger` has no location dimension). Implementation stays at Future-direction item 5 (after UI/UX phase) per the standing sequence, but the DESIGN is pulled forward so (a) the redesign phase builds screens that won't need re-layout for outlet switching, and (b) interim features stop conflating brand with location (forward-compat rule). Design must cover: outlet entity + relationship to brand; which records gain `outlet_id` (orders, shifts, cash movements, future per-outlet stock); whether inventory stays single-warehouse (current owner-confirmed reality — makes backfill trivial: all history belongs to outlet 1) or splits per outlet with transfer flows; POS outlet selection at session start; report dimensions (by outlet, by brand, consolidated); migration/backfill path and rollback. **Paused 2026-07-27** mid-brainstorm for `POS-OFFLINE-1` (POS offline resilience, see above) — that feature is now closed, so this should resume next. |
| [x] **WF-2. Per-item stock movement history view (owner-approved 2026-07-24)** | Claude Sonnet 5 | `app/admin/reports/stock/**` | Done 2026-07-24, commit `51e5f92`. Paginated (cursor-based, 30/page) per-item `Stock_Ledger` drill-down via a "Lịch sử" button on `/admin/reports/stock`. `tsc` clean, 721/721 tests, build passed. |
| **OPS-CONT-1. Operational continuity audit (account/credential recovery)** | Claude + owner (1 session) | No code — inventory doc + runbook in `docs/operations/` | Proposed 2026-07-24 (never audited in any gate): single-owner dependency on the Vercel/Supabase/Google/GitHub accounts. One session with the owner to inventory which accounts exist, what each controls (deploy, database, backups in Drive, repo), where recovery paths point (email/phone 2FA), and what happens if any one is lost — output is a plain-Vietnamese recovery runbook the owner keeps outside the system. No secrets stored in the repo. |
| [x] **BUILD-GATE-1. Production builds skip type checking and lint (owner-triggered structure audit 2026-07-24)** | Codex | `next.config.js`, `.eslintrc.json` | **Closed 2026-07-24 (commits `2914189`/`75dfdad`), reviewed by Claude Sonnet 5 2026-07-25.** `typescript.ignoreBuildErrors` flipped to `false` — a Vercel deploy of a type-broken tree now fails instead of silently succeeding. Baseline `.eslintrc.json` added (`next/core-web-vitals`); `eslint.ignoreDuringBuilds` deliberately left `true` pending a lint-backlog measurement pass, per the audit's own staged plan. Verified: `next build` still passes. |
| [x] **REPO-STRUCT-2. Structure hygiene batch + `lib/` lifecycle split (owner-triggered structure audit 2026-07-24)** | Claude Sonnet 5, fully closed 2026-07-27 | Root `migrations/` (deleted), `check-ts.js` (deleted), `package.json` (`ts-morph` removed, `dotenv` moved to devDependencies) | **RS-1/RS-2/RS-6 all closed 2026-07-27, commit `07ba68c`** (local only): deleted the legacy root `migrations/` folder (019-023, pre-CLI chain -- grep-verified zero code references, only hit was a frozen historical audit manifest's descriptive text); deleted `check-ts.js` (confirmed unreferenced by any script, hook, or code); removed `ts-morph` from `dependencies` (zero real imports anywhere, only doc/lockfile mentions) and moved `dotenv` to `devDependencies` (confirmed used exclusively by `scripts/` CLI tooling via `vite-node`, never at Next.js runtime); ran `npm install` to regenerate the lockfile (7 packages removed). Verified per `superpowers:verification-before-completion`: ran `tsc`/full suite/build fresh, then independently confirmed on disk that all 3 targets were actually gone before claiming done. RS-3 (`scripts/` disposition cadence) remains a standing monthly item, first run at next phase close. **RS-2 (the `lib/history-ops/` moves) done 2026-07-27.** Batch 1 (2026-07-24, commit `eed3392`): the only 3 candidates with zero importers anywhere outside their own test (`gate4-mac-drift-classification.ts`, `negative-stock-resolution.ts`, `purchase-cost-recovery.ts`). Batch 2 (2026-07-27, commits `6fe92e0`/`2eb6d70`, local only): the remaining 11 module files (`hong-luc-migration` group x3, `btp-shortfall-reprocess`, `cogs5-pipeline-audit`, `mac-drift-baseline`, `recovery-snapshot`, `task-3-recovery`, `btp-drift-lock`, `backdated-historical-gap-lock`, `migrate-v1-to-v2`) + their 11 test files, plus every `scripts/`-relative and `@/lib/` absolute import that referenced them across 9 `scripts/` files and 7 `lib/` files — previously blocked because it required a same-commit `scripts/` edit (Codex-exclusive), unblocked now that Sonnet 5 is the project's sole active agent (owner decision 2026-07-27). A second grep pass after the first round of path fixes caught 2 things a naive path-substitution would have missed: `hong-luc-migration.ts`/`btp-shortfall-reprocess.ts` use *relative* imports to engine files that stayed in `lib/` root (needed `../`, not a straight copy of the old path), and 2 files had a second absolute import to their own sibling module the first grep didn't catch. `void-order-reversal.ts` and the 3 explicit Engine Files (`mac-cogs-audit.ts`/`cogs-drift-audit.ts`/`purchase-ledger-rebuild.ts`) re-confirmed still live, correctly not moved. `tsc` clean, 759/759 (unchanged count), build passed. **Remaining in this row's scope**: RS-1 (delete root `migrations/`, still present, needs a fresh zero-reference check before deleting), `check-ts.js` (still present, needs a fresh unreferenced-wrapper check), `package.json`'s `ts-morph`/`dotenv` (still as originally found) — none touched yet this pass, all straightforward next candidates now that nothing blocks them. RS-3 (scripts/ disposition cadence) still a standing monthly item, first run at next phase close. |
| **INFRA-UPGRADE-1. Next.js 14→16 upgrade project (decision recorded 2026-07-24, not started)** | Codex, coordinator review, owner go-ahead required | `package.json`, `next.config.js`, `lib/sheets_db.ts` caching layer, React 18→19, `postcss` | Part 2 of `docs/audits/2026-07-24-repo-structure-audit-and-infrastructure-plan.md`. Infrastructure verdict recorded there: **language (TypeScript), hosting (Vercel), database (Supabase) all stay** — no rewrite, no platform move; Supabase Pro (~$25/mo) recommended before the multi-outlet phase for point-in-time recovery (pairs with the F-4 restore drill). The one real project is this upgrade: absorbs `DEP-1`'s remaining `next` advisories (fixed only in next@16), engine-adjacent because `unstable_cache` semantics change (Cache Components / `use cache`) and `lib/sheets_db.ts` is built on it. Full regression bar: `tsc`, full suite, `next build`, P&L/MAC 0-delta audit, live smoke. Schedule after the current P1 queue clears. Tailwind 3→4 explicitly out of scope. |

### P3 — Depends on verification

| Task | Owner | Notes |
|---|---|---|
| **V1. First real operator backdate verify** | Claude | Wait for operator to backdate PO (frequency: weekly per user interview). Walk through UI: list → detail → approve → verify drift = 0. |

### Blocked — needs decision or unblock

| Task | Blocker | Resolution path |
|---|---|---|
| (none) | — | SEC-5 resolved 2026-07-18 (owner: stock adjustment is admin/manager responsibility, staff should not submit) — folded into Wave 1, see P0. |

## Future direction (owner priority, set 2026-07-18/19 — not started, sequencing only)

Owner-stated long-term direction, in order. Nothing below starts until the
phase before it is done; do not begin implementation on any of these without
a fresh, explicit go-ahead even after the prior phase closes — this section
records intent and order, not authorization to start.

1. **Finish current work** — the eight-gate audit in progress (Gate 4 running
   as of 2026-07-19; Gates 5-8 follow). See P0/P1 above.
2. **Repository file/folder reorganization** — done 2026-07-20 in full;
   see `docs/superpowers/plans/2026-07-20-post-audit-repository-reorganization.md`
   for the proposal and `docs/COMPLETED.md` for what was executed (owner
   approved each item individually, including both items initially
   deferred as lower-priority: the 33-script `ARCHIVE_DOC_ONLY` batch
   was archived into `docs/audits/archive-scripts.md`, and the stale
   `.gitignore` patterns that contradicted the current full-tracking
   policy were removed). Remaining, genuinely low-value: a handful of
   non-date-prefixed `docs/audits/`/`docs/reports/` files that already
   self-declare their historical status in-file (not worth renaming
   retroactively).
3. **Feature-completeness pass** — plan and close gaps so the single-shop
   system fully covers: inventory control (kiểm soát hàng tồn), cash
   in/out control (kiểm soát tiền vào tiền ra), sales reports (báo cáo bán
   hàng), order reports (báo cáo đặt hàng), financial reports (báo cáo tài
   chính), and stock reports (báo cáo tồn kho). Likely overlaps
   significantly with Pre-Audit C's `FEATURE-CATALOG.md` findings and the
   deferred 17-section F&B checklist — reconcile rather than duplicate when
   this phase starts.
4. **UI/UX upgrade and frontend unification** (đồng nhất frontend) — after
   feature completeness, not before; a consistent UI on top of incomplete
   features would need rework.
5. **Multi-branch management** (đa chi nhánh) — first of the two expansion
   features. Needs outlet entity, data isolation, outlet-scoped roles,
   consolidated reporting design (see `docs/FEATURE-CATALOG.md`
   `ORG-MULTI-OUTLET` and the F&B spec's "Organization, brand, outlet, and
   device setup" checklist section).
6. **Full permissions and security hardening** (phân quyền và bảo mật) —
   done once the system's shape through multi-branch is known, to avoid
   designing the permission model twice for that part. This is distinct
   from Gates 1-3 (P0 exposures, access map, database/RLS — all closed) —
   those stay scoped to the current single-shop system; this phase is the
   full `docs/ACCESS-MODEL.md` Phase 3 verification plus whatever
   multi-branch roles add.
7. **Franchise management** (nhượng quyền) — moved to last (owner decision
   2026-07-19): not yet certain this gets built at all, so it shouldn't
   hold up or need to be designed around by anything before it, including
   the security hardening phase. If franchise is later approved, its
   franchisee role/tenant-isolation needs will require their own follow-up
   security review at that time — accepted tradeoff for not blocking on an
   uncertain feature. Needs franchisee role, fee/royalty model, and
   stronger tenant isolation than plain multi-branch, whenever it starts.

## Out of scope (do not start without explicit approval)

- **Negative stock recovery** — needs physical count decision from user. Current live state per Gate 5's fresh rerun (2026-07-19, independently verified by Claude): **3 items negative** — `ING-003` Sữa đặc -201 g (growing from -131 g during real sales while Gate 5 was in progress — live business activity, not caused by any Gate 4/5 change), `ING-021` -729.821 g (existing), `ING-024` -150 ml (existing). The previously tracked `ING-001`, `NNL-003`, `NNL-006` no longer appear in the live negative list — likely resolved since they were last logged here, not reverified as fixed; don't assume clean without checking if they come up again.
- **Franchise system** — see "Future direction" above; comes after multi-branch, needs design + business rules (multi-tenant RLS, franchisee role, outlet management)
- **Multi-branch system** — see "Future direction" above; comes after the feature-completeness pass and UI/UX unification, needs design + business rules (outlet entity, data isolation, outlet-scoped roles)
- **Historical data rewrite** — any rewrite of pre-2026-07 data requires explicit user approval + dry-run + atomic transaction
- **Auth system overhaul** — placeholder "admin" reviewer in backdate UI is a known gap, but full auth is separate scope; see "Future direction" item 6, deliberately last
- **Gates 4-8 of the full audit** — Gate 3 Phase A closed 2026-07-19 (see `COMPLETED.md`). Gate 3 Phase B findings (G3-A4 through G3-A8, see P2) are logged but not yet handed off — live database configuration changes need their own reviewed migration + availability test, not a quick fix. Gate 4+ not yet scoped. See `docs/superpowers/specs/2026-07-17-full-system-audit-program.md`.
- **17-section F&B capability checklist** — deferred from Pre-Audit C; needs owner per-item priority classification when scheduled.

## Pending prompts in `docs/handoffs/`

These prompts are ready for agents to pick up. Prompts for completed tasks remain as historical record.

**Update 2026-07-27: this list is fully historical.** Codex closed `DEP-1`, `INV-COUNT-1` (both phases), `AUDIT-TOOL-1`, `PERF-2` Phase A, `BUILD-GATE-1`, `REV-4`, and root-caused `COGS-5`'s open pipeline question in its 2026-07-24 evening session, then hit its usage limit again while planning `PERF-2` Phase B (2026-07-27). Owner declared Codex out indefinitely ("chưa biết khi nào quay lại... đừng đợi nó quay lại") and Sonnet 5 the project's sole active agent, including the independent-review role `REV-2`/`REV-3`/`REV-5` were waiting on. All three were self-reviewed and closed 2026-07-27 (see rows above) -- re-verified with fresh, skeptical re-reads rather than a rubber stamp, live-audited against production, and in two cases corroborated by evidence Codex itself had already built on top of the reviewed code without issue. If Codex becomes available again, an actual independent re-check of this window's self-reviews would still be worthwhile, but nothing is blocked on it. Remaining queue: `PERF-2` Phase B is done (materialized inventory balance, see its row above); `ARCH-1` (design-only, stays last per the standing sequence) and `INFRA-UPGRADE-1` (needs owner go-ahead) are the two items left in this section, neither blocked by Codex's absence specifically.

- `2026-07-24-codex-inv-count-1-s2-confirm-and-apply.md` → `INV-COUNT-1` phase S2 — confirm-and-apply RPC writing `STOCK_ADJUST`, review migration `0036` first — ready for Codex pickup, sequence per the priority order above
- `2026-07-24-wf1-purchase-history-and-search.md` → WF-1 — per-item purchase history, PO item-name search, supplier→PO links — historical reference, work complete (commit `dd91596`)
- `2026-07-24-antigravity-ui-clean-1.md` → UI-CLEAN-1 — design-free cleanup sweep — historical reference, work complete (commits `27b99aa`/`198c035`/`22df610`/`38b8d0f`), except `app/admin/orders/actions.ts`'s 4 "Unknown" strings left for Codex
- `2026-07-24-codex-wave3-performance-remediation.md` → PERF-2 — POS dead-work strip, activity-log pagination, PERF-1 fix, void/edit ledger bounds + stock-balance design proposal — ready for Codex pickup per the priority order above
- `2026-07-22-antigravity-fc2-reorder-suggestion-ui.md` → FC-2 — low-stock/reorder-suggestion UI, historical reference — owner decided to skip the Antigravity handoff given the task's simplicity; Claude built it directly instead (see `docs/COMPLETED.md`)
- `2026-07-20-antigravity-fc1-split-payment-pos-ui.md` → FC-1 — POS split/mixed payment UI — historical reference, work complete (Antigravity `d631b10`, reviewed + fixed by Claude Sonnet 5 `dd4cada`)

- `2026-07-20-codex-gate8-regression-final-acceptance.md` → Full audit Gate 8 — regression and final acceptance (last gate) + `notify-order` Supabase undeploy — ready for Codex pickup, P1
- `2026-07-19-codex-gate7-performance-operations-audit.md` → Full audit Gate 7 — performance/operations audit — historical reference, work complete and Claude-reviewed (commits `86f39ec`-`c7c219a`, merged `2a5798a`)
- `2026-07-19-antigravity-gate6-accessibility-audit.md` → Full audit Gate 6 — UI/UX/accessibility audit — historical reference, work complete and Claude-reviewed (commits `642bea8`-`a14b8e1`)
- `2026-07-19-codex-gate5-pos-checkout-idempotency.md` → Full audit Gate 5 — POS checkout idempotency — historical reference, work complete and Claude-reviewed (commits `c1f1b04`-`3291d70`, merged)
- `2026-07-19-codex-rev1-script-fix-crosscheck.md` → REV-1 — cross-check 2 pre-rule script fixes — historical reference, work complete and Claude-reviewed (commit `39ea6c2`, merged)
- `2026-07-19-codex-gate3-phase-b-database-hardening.md` → Gate 3 Phase B — database grant/function hardening (G3-A4/A5/A6/A8) — historical reference, work complete and Claude-reviewed (commit `58aa46a`, merged `d7cabb4`)
- `2026-07-19-codex-fix1-password-fix2-remove-backup-page.md` → FIX-1 (password change) + FIX-2 (remove Backup&Sync page) — historical reference, work complete and Claude-reviewed (commit `fe04f4a`)
- `2026-07-19-codex-gate4-phase-b-atomic-rpc-remediation.md` → Gate 4 Phase B — atomic RPC remediation, historical reference, work complete and Claude-reviewed (commits `c6c61b7`/`31cee7f`/`576572b`/`22823ce`/`016bed6`, merged `bb2983c`)
- `2026-07-19-codex-gate4-order-inventory-cogs-audit.md` → Full audit Gate 4 Phase A — historical reference, work complete and Claude-reviewed (commits `c0be7ce`, `15e3889`, `26b2eb8`, `159b7c9`)

- `2026-07-19-codex-gate3-database-rpc-rls-audit.md` → Full audit Gate 3 Phase A — historical reference, work complete and Claude-reviewed (commit `a17b0e7`)
- `2026-07-18-antigravity-ui-remed-1-visual-smoke-test.md` → UI-REMED-1 visual smoke test — historical reference, work complete and Claude-reviewed (commit `2cabde9`)
- `2026-07-18-codex-gate2-remediation-wave1-pos-system-actor.md` → Gate 2 Remediation Wave 1 — historical reference, work complete and Claude-reviewed (commits `5c4a01b`, `a1ace53`)
- `2026-07-18-codex-gate2-remediation-wave2-admin-reads.md` → Gate 2 Remediation Wave 2 — historical reference, work complete and Claude-reviewed (commits `a20aba8`, `79bda17`, `71b319f`)
- `2026-07-18-codex-gate2-access-map.md` → Full audit Gate 2 — historical reference, work complete and Claude-reviewed (commits `3570da0`, `f14b092`)
- `2026-07-17-codex-gate1-p0-security-exposures.md` → Full audit Gate 1 — historical reference, work complete and Claude-reviewed (commits `dd2f970`, `57d298a`, `9a8ee66`)
- `2026-07-17-codex-pre-audit-c-feature-inventory.md` → Pre-Audit C — historical reference, work complete and Claude-reviewed (commit `99f466d`)
- `2026-07-17-codex-pre-audit-b-execution.md` → Pre-Audit B Execution — historical reference, work complete and Claude-reviewed (commits `7c2409b`, `b238411`, `caacc58`)
- `2026-07-17-codex-pre-audit-b-canonical-proposal.md` → Pre-Audit B proposal — historical reference, work complete (commit `8016ae8`)
- `2026-07-17-codex-pre-audit-a-documentation.md` → Pre-Audit A — historical reference, work complete (commit `f12725f`)
- `2026-07-17-antigravity-pos-redesign-1-session-3.md` → POS-REDESIGN-1 Session 3 — historical reference, work complete (commit `20a1d38`)
- `2026-07-17-antigravity-pos-redesign-1-session-2.md` → POS-REDESIGN-1 Session 2 — historical reference, work complete (commit `c61f5a1`)
- `2026-07-17-antigravity-pos-redesign-1-session-1.md` → POS-REDESIGN-1 Session 1 — historical reference, work complete (commit `a3682db`)
- `2026-07-17-antigravity-ui-remed-6-remove-stickybar.md` → UI-REMED-6 — historical reference, work complete (commit `7eecf7e`)
- `2026-07-17-antigravity-ui-remed-1-token-swap-overnight.md` → UI-REMED-1 — historical reference, work complete (5 phases: `c33033f` + `8f93742` + `d239cbb` + `55ef69d` + `ee33450`)
- `2026-07-17-antigravity-ui-remed-5-polish.md` → UI-REMED-5 — historical reference, work complete (commit `11c566b`))
- `2026-07-17-antigravity-ui-remed-4-boundaries.md` → UI-REMED-4 — historical reference, work complete (commit `c923086`)
- `2026-07-17-antigravity-ui-remed-3-session-2.md` → UI-REMED-3 Session 2 — historical reference, work complete (commit `2f91b3f`)
- `2026-07-17-antigravity-ui-remed-3-session-1.md` → UI-REMED-3 Session 1 — historical reference, work complete (commit `dd51dae`)
- `2026-07-16-antigravity-ui-remed-2-sticky-filter-bar.md` → UI-REMED-2 — historical reference, work complete (commit `6b65aba`)
- `2026-07-16-codex-task-3.10-audit-display.md` → Task 3.10 — historical reference, work complete (commit `6a5bdec`)
- `2026-07-16-codex-task-3.5-cohort-aware-audit.md` → Task 3.5 — historical reference, work complete (commit `c28319d`)
- `2026-07-16-codex-task-3.9-historical-gap-lock.md` → Task 3.9 — historical reference, work complete (commit `09bf26a`)
- `2026-07-16-codex-task-3.8-backdated-events-surface.md` → Task 3.8 — historical reference, work complete (commit `ad7f7ba`)
- `2026-07-16-codex-task-3.7-btp-drift-lock.md` → Task 3.7 — historical reference, work complete (commit `d2177ca`)
- `2026-07-15-codex-task-3.6-forward-drift-investigation.md` → Task 3.6 — historical reference, work complete (commit `d32d4d4`)
- `2026-07-15-codex-task-3.4-outside-cohort-investigation.md` → Task 3.4 — historical reference, work complete (commit `fea097d`)
- `2026-07-09-codex-modifier-recipe-hardening.md` → E1 (Task 1) — historical reference, work complete
- All other prompts in `docs/handoffs/` reference completed work — see `COMPLETED.md` for outcomes

## Quick links

- Completed work archive: `docs/COMPLETED.md`
- Detailed chronicle log: `DEVELOPMENT-TRACKING.md`
- Protocol: `docs/COLLABORATION.md`
- Feature evidence contract: `docs/FEATURE-CATALOG.md`
- Business rule index: `docs/BUSINESS-RULES.md`
- Access intent and verification boundary: `docs/ACCESS-MODEL.md`
- Test strategy: `docs/TESTING.md`
- UI audit reference: `docs/audits/2026-07-06-ui-consistency-audit.md`
- MAC drift baseline: `docs/audits/2026-07-09-mac-drift-baseline-audit.md`
- Backdated ledger investigation: `docs/audits/2026-07-09-prod-028-btp-shortfall-investigation.md`
- Backdated cost events playbook (read before touching any unflagged mismatch): `docs/operations/backdated-cost-events-playbook.md`
- **Full system audit program** (future, owner-triggered): `docs/superpowers/specs/2026-07-17-full-system-audit-program.md`

## Change log

- 2026-07-27 Claude Sonnet 5: fixed an urgent production bug reported by the owner mid-session (screenshot: standalone topping products showing up in the Sales report's "Top sale - Nước" table instead of "Top Topping Bán Chạy"). Root cause: `buildStandaloneToppingMap` (`app/admin/reports/actions.ts`) only mapped a CAT-007 product into the topping bucket when its `migration_notes` carried a linked modifier id -- every CAT-007 product actually in production has no such link, so the entire routing mechanism was silently inert, a regression against the approved 2026-06-27 classification spec (whose own reference code always falls back to the product's own id). Fixed by restoring that fallback; cascades correctly through both the Sales and P&L call sites that share the helper. Added a regression test reproducing the exact production shape, confirmed it fails pre-fix and passes post-fix. `tsc` clean, 751/751, build passed. Committed and **pushed to origin/main** immediately per the owner's explicit priority request (commit `8a4716e`).
- 2026-07-27 Claude Sonnet 5: owner reported Codex hit its usage limit again mid-session (this time while planning `PERF-2` Phase B execution, right after re-confirming approach 1 with the owner) and declared Sonnet 5 the project's sole remaining agent going forward -- a step beyond the earlier time-boxed "until Codex returns" exceptions, extending to `lib/*.ts`/`supabase/migrations/*.sql`/`scripts/*.ts` and self-review with no stated end condition. Updated the active-agents table accordingly. Picked up Codex's own already-written, owner-approved design (`docs/superpowers/specs/2026-07-25-materialized-inventory-balance-design.md`) and implementation plan (`docs/superpowers/plans/2026-07-25-materialized-inventory-balance.md`) and executed it task-by-task exactly as planned: migration `0038` (new `inventory_balances` table + trigger, commit `18bafdd`), a read-only drift audit (commit `cec3ab7`), and the `getRealtimeStock`/`getPOSStockStatus` read-path switch (commit `87997f3`) -- catching and correcting one real defect in the plan itself along the way (its Task 3 instruction to drop the `sheets-Stock_Ledger` cache tag would have silently broken cache invalidation, since nothing else in the app ever calls `revalidateTag` for the new table name). All 3 commits verified (`tsc` clean, 757/757, build passed) but held back from both push and production: migration `0038` is confirmed absent from remote via `supabase migration list` (which also confirmed `0036`/`0037` are already applied). Waiting on the owner to run `supabase db push` before this can deploy -- pushing the code first would break both stock-read paths against a table that doesn't exist yet.
- 2026-07-25 Claude Sonnet 5: owner reported Codex hit its usage limit again and is unavailable for a while; Sonnet 5 now covers everything (analysis, planning, execution, self-review) until Codex returns, same time-boxed exception pattern as the earlier `REV-2`–`REV-4` window. Reviewed all of Codex's 2026-07-24 evening work (17 commits): closed `DEP-1` (vitest 4 upgrade), `PERF-2`, `AUDIT-TOOL-1` rebuild, `BUILD-GATE-1`, `REV-4`, and root-caused `COGS-5`'s open pipeline question (the "second-event" premise was disproved — real defect was a zero-change event never marked settled, not a cost error). All read as solid, well-tested, correctly-scoped work — no issues found in Codex's own changes. Found one gap while reviewing: `voidOrderV2`'s implicit-production reversal fix (Codex commit `4f6ba40`) didn't get applied to the sibling `editOrderV2` path, which had the identical pre-existing bug (confirmed via git history it predates this session). Fixed directly (commit `025abea`, reusing the same `lib/void-order-reversal.ts` helper), verified `tsc`/745 tests/build, and re-ran the full-history ledger audit live afterward (0 mismatches, down from the 6 Codex's audit found — the void-path repair appears already applied to production, confirm with Codex when back). Logged `REV-5` for Codex's own return-review of this window's work. Updated `docs/ROADMAP.md` P1/P2 rows for everything closed today and replaced the stale "Codex priority order" note.
- 2026-07-24 Claude (Fable): owner asked two questions — (1) audit the directory structure and propose the right target structure, (2) plan the infrastructure direction (language, keep Vercel or switch). Wrote `docs/audits/2026-07-24-repo-structure-audit-and-infrastructure-plan.md` (read-only survey, no files moved): 6 structure findings (RS-1 legacy root `migrations/` shadow chain, RS-2 flat `lib/` mixing engine with closed one-off modules, RS-3 `scripts/` regrew 133→186 in 4 days with no disposition cadence, RS-4 production builds skip tsc/lint via `next.config.js` ignore flags — the one real defect, RS-5 root hygiene, RS-6 unused/misplaced deps) and the infrastructure verdict: TypeScript/Vercel/Supabase all stay, no rewrite, Supabase Pro before multi-outlet for PITR, Next 14→16 as its own project (`INFRA-UPGRADE-1`), auth replacement decision deferred to the overhaul with both candidates recorded for `ARCH-1`. Added P2 rows `BUILD-GATE-1`, `REPO-STRUCT-2`, `INFRA-UPGRADE-1`. All moves/deletes remain propose-then-approve per D8.
- 2026-07-24 Claude Sonnet 5: owner tightened the Codex/Sonnet split — engine-critical write paths and `supabase/migrations/*.sql` stay fully with Codex (implementation + review), not split with Sonnet even under a plan-approved exception. Reassigned `INV-COUNT-1` phase S2 to Codex end to end and flagged S1's already-drafted migration `0036_stocktake_sessions.sql` for Codex review (rewrite option open, no production usage yet). Wrote `docs/handoffs/2026-07-24-codex-inv-count-1-s2-confirm-and-apply.md` and recorded a single priority-ordered list for Codex's queue (vitest upgrade → REV-2/3/4+COGS-5 → INV-COUNT-1 → AUDIT-TOOL-1 → PERF-2 → ARCH-1) in the pending-prompts section.
- 2026-07-24 Claude Sonnet 5: owner expanded Sonnet 5's scope to absorb all UI work previously assigned to Antigravity ("tất cả scope đang phân quyền cho agy cũng sẽ do em xử lý" / "agy chỉ là dự phòng" — Antigravity is backup-only now). Updated the active-agents table, reassigned `FC-1`'s pending UI piece and `UI-CLEAN-1` to Claude Sonnet 5, added `FC-1`'s UI piece as its own P1 row (it existed only as a dangling handoff reference before, not in the priority queue table). See `docs/COLLABORATION.md` change log for the matching protocol update.
- 2026-07-22 Claude: closed FC-2 end-to-end. Owner asked which of Claude/Antigravity would do the UI piece better; answered honestly there's no real capability gap for a task this simple (extend an existing table, functional only) — the Antigravity assignment was a workflow/independent-review convention, not a quality requirement. Owner decided the review overhead wasn't worth it here and asked Claude to build the UI directly instead of waiting on the handoff. Built `components/ReorderSuggestionTable.tsx` (low-stock flag, computed reorder point, suggested quantity in purchase units, "not enough data" state, toggle to show only items needing reorder) wired into `/admin/reports/stock`. `tsc` clean, full suite 650/650, `next build` passed; could not browser-verify directly (no login session available in this environment) so left a `next dev` instance running on port 3001 for the owner to check visually. Moved `FC-2` to `docs/COMPLETED.md` in full (was previously split: backend closed, UI handed off) — feature-completeness pass is now 2 of 3 done, only `FC-3` (shift/cash reconciliation) remains. Flagged in `REV-4` that Codex's retroactive review should also sanity-check the UI, since it skipped Antigravity's normal independent review.
- 2026-07-22 Claude: closed FC-2's backend/logic, the second of the 3 owner-confirmed feature-completeness items (design: `docs/superpowers/plans/2026-07-20-feature-completeness-required-now-roadmap.md` section 2). New `lib/reorder-suggestion.ts` — a pure function `computeReorderSuggestions` (data fetching kept in the caller, matching `lib/full-history-recompute.ts`'s convention) that computes, per active base ingredient/semi-product: avg daily consumption from `SALES_CONSUME`+`PRODUCTION_CONSUME` over a 14-day lookback; lead time derived from completed `Purchase_Orders`' creation timestamp vs. the matching `PO_RECEIPT` ledger row's timestamp (falls back to a 3-day default when no PO history exists for that item); reorder point = avg daily consumption x lead time x 1.3 safety buffer; suggested reorder quantity = 10-day target coverage x avg daily consumption minus current stock, floored at 0, converted to purchase unit via `UOM_Conversions` when available. Items with under 3 consumption events in the lookback window report "not enough data" instead of a falsely-confident number, per the owner's explicit requirement. New server action `getReorderSuggestions()` in `app/admin/inventory/actions.ts`, same `requireAdmin()`/`unstable_cache` pattern as the existing `getRealtimeStock()`. Read-only throughout — no new write path, no schema change. 9 new unit tests, `tsc --noEmit` clean, full suite 650/650 (up from 641), `next build` passed. Wrote the UI piece as a handoff for Antigravity (`docs/handoffs/2026-07-22-antigravity-fc2-reorder-suggestion-ui.md`) rather than building it myself, per the same owner-confirmed UI/backend split used for FC-1. Logged `REV-4` for Codex's retroactive review.
- 2026-07-21 Claude: owner pushed back correctly on cost-accuracy sequencing (asked how cost could be verified without stock-quantity accuracy first) -- confirmed via a real timeline check that the 209 known quantity mismatches genuinely can and do skew MAC cost calculations for the same items. Investigated root cause: 209 mismatches split into 90 lines/77 orders matching the exact pre-2026-07-20 "direct raw debit" bug (Round 1 only caught orders tagged `BTP_SHORTFALL`, missing this population), plus 119 lines from a separate, likely low-value cause (e.g. untracked zero-cost ingredients like Nước sôi). Removing Round 1's tag filter to catch the missed population, expecting ~77 orders, instead matched 992 orders (10,054 new ledger rows) -- turned out nearly every historical semi-product sale used the old bug pattern, not a rare exception. Applied it, then immediately re-ran the standard verification audits per the playbook's own rule and caught a real regression before declaring success: mismatches went 209 → 2,853, not down. Root-caused within the hour: the correction logic assumed a single recorded-ledger shape that didn't hold for orders already touched by an earlier, unrelated correction. Rolled back cleanly using each row's own tag+timestamp to distinguish Round 2 from Round 1's legitimate 479-order fix; reverified back to the exact prior baseline. Wrote up the full incident and the corrected approach needed before retrying (`docs/operations/implicit-production-quantity-correction-playbook.md`, `COGS-3`). Also added 2 durable behavioral rules to `CLAUDE.md` per direct owner feedback this session: always use real product/ingredient names in conversation (never internal DB codes), and proactively flag known cross-impacts between sequential fixes instead of waiting to be asked.
- 2026-07-21 Claude: closed the 2 remaining known cost-accuracy gaps. (1) `COGS-2` (migrated-order MAC accuracy, logged earlier tonight as "unquantified"): investigated in depth, found the earlier "926 lines unverifiable" claim was wrong (a diagnostic script bug checking a nonexistent field name) -- all 1,038 migrated lines were actually checkable, and the real 214-line/+438,131 VND correction had already been applied via `scripts/apply-migrated-orders-mac-correction.ts`. Corrected the record with the owner immediately on discovering the error. (2) The Task 3.9 41-line `BACKDATED_LEDGER_HISTORICAL_GAP` lock cohort (2026-07-16, -43,809 VND, previously left locked under owner's Option A "accept as drift" decision): owner explicitly reversed that decision tonight given the fresh context established earlier this session (PO-receipt timestamps are accurate dates with placeholder times, goods always arrive before sales) -- applied the already-approved recovery plan via the purpose-built `apply_mac_drift_recovery` RPC (migration 0016), using the exact `stored_cost_at_sale`/`expected_cost_at_sale` values already recorded in `audit_baseline_locks` (no fresh recompute needed, the 2026-07-16 review already computed and Claude-approved them). `scripts/apply-task-3.9-historical-gap-recovery.ts`. Reverified after both: `audit-pnl-mac-consistency.ts` 0 VND delta (22,904,406 VND total COGS), `verify-all-479-clean.ts` unchanged 209-mismatch quantity baseline (cost corrections don't touch stock quantity). All cost-accuracy gaps identified this session are now closed.
- 2026-07-20 Claude: closed FC-1's backend/logic (commit `17a191e`), the first of the 3 owner-confirmed feature-completeness items. Migration `0024` adds `order_payments` and extends `create_pos_order_atomic` with an optional `p_payments` array, validated inside the transaction to sum to `net_total`; fully backward compatible (omitting it synthesizes the existing single-payment behavior). Updated `lib/order-cart.ts` to validate and build the payments array client-side before the order is ever built, and `RPT-SALES`'s payment breakdown to attribute revenue per payment line instead of per order. Applied the migration to production after the owner ran `supabase db push` themselves (blocked at the tool-permission layer by design — asked the owner to run it via `!`, rather than loosen my own deny rule). Live-probed against production: split payment persisted and summed correctly, a mismatched sum was rejected before any write, and a legacy caller with no `p_payments` still produced the expected synthesized row — 0 residual rows after cleanup. Reconfirmed P&L/MAC consistency and the order-ledger audit unchanged. `tsc` clean, suite 570/570 (up from 562), build passed. Wrote the remaining POS UI piece as a handoff for Antigravity (`docs/handoffs/2026-07-20-antigravity-fc1-split-payment-pos-ui.md`) rather than building it myself, since the owner explicitly assigned UI to Antigravity and wants it functional-only, deferred styling to the later redesign phase. Logged `REV-3` for Codex's retroactive review.
- 2026-07-20 Claude: owner picked up the 2 items deliberately deferred from the reorganization's first pass. Cross-referenced all 33 `ARCHIVE_DOC_ONLY` scripts for real code dependencies (same rigor as the earlier `DELETE_ONE_OFF` pass) — none had any, except `batch-sheets-orders.ts`'s existing dependents (already being kept regardless). Read each script's actual code/comments (not just its filename) to write an honest one-line description per script in the new `docs/audits/archive-scripts.md`, then removed all 30 genuine one-off files from `scripts/` (full source stays in git history). Caught the classifier's own self-referential false positive along the way (`generate-script-cleanup-plan.ts` and its `-core` files matching "cleanup-" as a substring of their own name) and excluded those 3 from the archive. Removed the stale `.gitignore` patterns from Stabilization Phase 3 that silently excluded future one-off scripts from git — confirmed nothing currently on disk matched them first, so this only changes behavior for scripts created going forward. `tsc` clean, suite 562/562, build passed after each commit. Reorganization phase now fully closed — 133 scripts remain, down from 220.
- 2026-07-20 Claude: owner gave the explicit go-ahead for the repository reorganization pass ("Future direction" item 2). Wrote a full proposal before touching anything (`docs/superpowers/plans/2026-07-20-post-audit-repository-reorganization.md`), per D8's "no file gets moved or deleted without explicit owner approval." Found and fixed a classifier bug first (`lock-*`/`recover-*` scripts misclassified as deletable, contradicting `docs/FILE-ORGANIZATION.md`'s own rule — 2 of the 4 affected have real test dependents). Cross-referenced all 62 `DELETE_ONE_OFF` candidates against actual code/lib/doc dependencies rather than trusting the naming heuristic alone. Owner approved each item individually via explicit confirmation: deleted 56 scripts, deleted `TASK.md`, merged `docs/runbooks/` into `docs/operations/`, moved 2 misplaced handoff files into `docs/handoffs/` with updated references in `CLAUDE.md`/`AGENTS.md`/`docs/COLLABORATION.md`. Deferred the `ARCHIVE_DOC_ONLY` batch and a newly-found `.gitignore`/policy conflict to a later decision rather than scope-creeping them in. `tsc` clean, suite 562/562, build passed throughout. Moved to `COMPLETED.md`.
- 2026-07-20 Claude: owner authorized picking up outstanding Codex-owned work directly until Codex's rate limit resets 2026-07-25 ("làm luôn việc của Codex"), broader than the single pre-approved audit-admin-action-auth fix — saved to memory as a time-boxed exception, not a permanent change; `REV-2` (Codex reviewing Claude's own work) explicitly stays with Codex regardless, since self-review there would defeat the point. Fixed `SCRIPT-BUG-1` (commit `f96db7e`): same `-core.ts` extraction pattern, fixed both the counting logic's stale `po_id` column and the sample-printing logic's separately-wrong `l.qty` field name, added 6 tests, live rerun now reports 0/55 mismatches (previously 55/55 false positives). While investigating `SEC-4`, discovered `npx supabase functions list` already exposes the `verify_jwt` deployment flag directly (no dashboard access needed after all) — resolved conclusively: `user-admin` is `verify_jwt: false`, but its `/migrate` route already uses a constant-time secret comparison (fixed and reviewed back in Gate 2 Remediation Wave 1, commit `a1ace53`) and all other routes independently verify via `admin.auth.getUser()` + require `role === 'owner'`, so the platform flag not being set doesn't leave a gap; `backup-to-sheets` is `verify_jwt: true` and also orphaned. Moved to `COMPLETED.md`.
- 2026-07-20 Claude: completed the rest of Gate 8 after the scripts/ ownership exception above. Ran the full consolidated regression sweep (14 read-only audit scripts across security/order/inventory/COGS/POS/backup domains) and compared every result against its last recorded baseline rather than just checking pass/fail — confirmed zero real regressions from Gates 1-7 (301 order-ledger mismatches unchanged despite +13 orders, same 3 negative-stock items with grown balances from ordinary sales, same 12-line MAC finding, same 15,000 VND `UCK000269` gap, 0 orders with a `client_request_id` explained by production still being pre-Gate-5 code). Found one new thing: `audit-po-save-ledger.ts` flags 55/55 completed POs as "mismatched" — traced this to the script using a stale `po_id` column name instead of the real `purchase_order_id` (predates tonight, likely from before the Supabase migration); verified independently with a throwaway script (outside `scripts/`, deleted after use) using the correct column name and got 0 missing/0 mismatch across all 55 POs, confirming real PO/ledger data is fully consistent and this is purely a tooling bug. Logged as `SCRIPT-BUG-1` for Codex rather than fixing it directly (unrelated to the pre-approved exception, routed through the normal ownership rule). Did a light cross-gate consistency check (grepped for stale `/admin/backup` references post-FIX-2 — none found). Wrote `docs/audits/2026-07-20-gate8-regression-final-acceptance.md` recording all of the above, the full Gates 1-7 status table, and the functional baseline SHA (`a0a07e4`) — explicitly did not declare the audit program "accepted," left that determination to the owner. `tsc` clean, suite 551/551, build passed.
- 2026-07-20 Claude: Codex hit its usage limit mid-investigation of Gate 8's stop-gate finding (audit-admin-action-auth.ts misclassifying `/api/client-errors` as `UNGUARDED_ROUTE`), rate-limited until 2026-07-25. Read the script directly and confirmed Codex's diagnosis exactly: `getRoutePolicy()` had no `AUTHENTICATED` tier for routes (only `ADMIN`), even though the action-level classifier already had this distinction — a tooling classification gap, not a real vulnerability (the route already correctly requires `resolveActor()`, returns 401 unauthenticated). This falls under the `scripts/` Codex-ownership rule (Claude doesn't self-edit scripts/, always hands off to Codex, to preserve independent review); asked the owner given Codex is unavailable for 5 days, owner approved Claude implementing the fix directly with Codex reviewing retroactively once back (`REV-2`, same pattern as `REV-1`). Extracted the pure classification logic into `audit-admin-action-auth-core.ts` (mirroring the existing `audit-gate3-database-security-core.ts` split so the fix is unit-testable without triggering the CLI's full filesystem scan), fixed the route-status check to mirror the already-correct action-level logic, added 11 tests including an explicit regression check that real `ADMIN` routes still reject `ACTOR`/`SESSION`-only guards. Commit `28bd508`. Verified: `tsc` clean, full suite 551/551, production build passed, live rerun of the audit script confirms the fix (exit code 0, `/api/client-errors` now `GUARDED`).
- 2026-07-19 Claude: reviewed, approved, and merged Gate 7 (commits `86f39ec`-`c7c219a`, no-fast-forward merge into `main`). Read every diff directly rather than trusting the report: confirmed the new `findAllWhereInBatches` scoping preserves exact filtering semantics for `getOrderDetailV2` (order-chain lookup logic unchanged, just narrower row fetches), `getPnLDataV2`/`getSalesDataV2` (brand/staff filters now pushed to the DB query but the original in-memory filter is still applied as a safety net, so behavior is provably unchanged), and `getPromotionPerformanceV2`. Independently reran everything rather than accepting a single claim on faith: the row-set-parity script live (exact match: 1,593→424 orders, 2,279→617 lines, 1,606→1 events), the P&L/MAC consistency audit (exact match: 1,572 orders, 22,146,149 VND, 0 delta), the frozen MAC baseline check (hash unchanged, 0 `LOCKED_VIOLATION_STORED`), and the live backup snapshot verifier (exact match: 32/32 tables, 14,901 rows, 8,320,416 bytes). Full suite (540/540) and TypeScript (clean), both on the branch and again after merging into `main`. Traced Codex's flagged "12 new MAC lines" back to the exact same 12 order-line IDs already resolved earlier the same day as the known `BACKDATED_LEDGER_LIKE` mechanism (`docs/audits/2026-07-19-gate4-mac-drift-12-line-classification.md`) — confirmed not a new issue, no action needed, noted in `COMPLETED.md` so it isn't re-investigated later. Updated `PROD-BUG-1`'s tracking row: the new client-error-logging endpoint means the next occurrence will be captured server-side once deployed, partially unblocking that investigation. Merged without a separate approval round per the standing overnight agreement (merge/next-task decisions don't require per-step sign-off; only push and genuine business tradeoffs do).
- 2026-07-19 Claude: owner asked to delete the Telegram order-report feature entirely (no longer needed). Confirmed it had zero application callers and zero database triggers anywhere in the repo (`EXT-TELEGRAM-ORDER` in `FEATURE-CATALOG.md` had already flagged it as caller-less) — a clean removal with no other code depending on it. Deleted `supabase/functions/notify-order/` entirely. Updated references: `FEATURE-CATALOG.md` (marked `REMOVED`), `ARCHITECTURE.md` (dropped the Telegram integration line), `docs/ACCESS-MODEL.md` (dropped Telegram tokens from the approved-secrets list), `docs/ROADMAP.md`'s `SEC-4` item (dropped `notify-order`, now only 2 functions need JWT-setting verification), and the Gate 7 handoff (dropped the option to reuse its pattern for error-logging, since the function no longer exists). Historical audit entries (Gate 2 access map, `DEVELOPMENT-TRACKING.md`) left untouched as point-in-time records.
- 2026-07-19 Claude: owner set explicit ongoing priority order (functions work well → no network congestion → speed → accurate data recording → data integrity → clean input processing) and asked to continue per the established gate sequence. Investigated a live production crash report in parallel: confirmed via Vercel function logs (project linked, `vercel logs`) that the error is client-side-only (zero server errors in the relevant window); confirmed production is 117 commits behind local `main` (last deployed 2026-07-16), so this bug predates tonight's work entirely. Found and fixed a real gap — `app/global-error.tsx` was missing, so root-layout-level crashes bypassed the app's existing Vietnamese error UI (`app/error.tsx`) and showed Next.js's raw fallback instead; added it (commit `866b0bd`) as an immediate mitigation, not a root-cause fix. Checked the 3 most likely root-layout-level culprits directly (`layout.tsx`, `SessionProvider.tsx`, `DialogHost.tsx`/`lib/dialog.ts`); none show an obvious bug via static reading. Logged as `PROD-BUG-1`, blocked on the user supplying the actual browser error text. Separately scoped Gate 7 (performance/operations) matching the owner's stated priorities almost exactly — found `findAll()`'s full-table-load pattern is a real, evidenced, worsening-over-time performance risk given transaction-table cache invalidates on every write, and confirmed a real operational blind spot (no remote error visibility) directly relevant to the still-open production bug.
- 2026-07-19 Claude: closed Gate 6 (commit `a14b8e1`, the contrast color fix). Independently recomputed both new contrast ratios by hand (verified the calculation method itself against a known public reference — pure middle-gray on white — before trusting it): new `text-muted` 4.756:1 (matches Antigravity's 4.74 claim closely), new `text-secondary` 7.58:1 (Antigravity claimed 6.9 — a real discrepancy, noted but immaterial since both clear the AA threshold comfortably). Reran suite (523/523) and TypeScript (clean). Gate 6 fully closed; moved to `COMPLETED.md`.
- 2026-07-19 Claude: reviewed Antigravity's Gate 6 mechanical-fix commit twice. First pass found the audit report's own claim ("all 10 icon-only buttons fixed") didn't match the diff — only 5/10 were actually done, plus an unverifiable claim about fixing a missing `</div>` with no trace in the diff. Reported this precisely (file-by-file table, not a vague concern) per the user's explicit request for strict impartiality. Antigravity corrected via `git commit --amend` (now `c92a1e7`); independently re-verified all 10 files fixed, reran suite/TypeScript/build clean. Approved the mechanical phase; flagged that keyboard-operability and color-contrast verdicts from the original handoff scope weren't explicitly reported and should be confirmed before Gate 6 closes fully.
- 2026-07-19 Claude: after Gate 5 closed, checked whether Codex had further Codex-appropriate work queued. Next gate in sequence (Gate 6, UI/UX/accessibility) is Antigravity's domain, not Codex's — asked the owner rather than either skip gate order (jump Codex to Gate 7) or silently leave Codex idle without explanation. Owner chose to keep gate order and engage Antigravity for Gate 6. Scoped it from a first-pass investigation (grepped the existing UI audit for accessibility coverage: zero; found a concrete example gap — 2 icon-only close buttons with no `aria-label` — and confirmed a good existing baseline, every `<img>` already has meaningful `alt` text). Authored `docs/handoffs/2026-07-19-antigravity-gate6-accessibility-audit.md`. Codex is idle pending this gate.
- 2026-07-19 Claude: reviewed, approved, and merged Gate 5 (commits `c1f1b04`-`3291d70`, migration `0023`). Read the migration and the client-side token/fingerprint logic in full, independently queried the live production database for probe residue after Codex's live idempotency test (found none), reran the suite (523/523), TypeScript (clean), migration list, and 4 live audits (order-ledger, current-stock, P&L MAC consistency, new POS-checkout-idempotency check) — all matched exactly. Merged without a separate approval round per the standing overnight agreement. Updated the negative-stock out-of-scope entry (`ING-003` now -201 g, live business activity during the gate, not a new issue).
- 2026-07-19 Claude: reviewed, approved, and merged REV-1 (commit `39ea6c2`). Independently traced the tightened reference-detection regex by hand against `.tsx`/`.ts.bak` false-positive cases, reran the suite (512/512) and TypeScript (clean) on the branch and again after merging, reran the live verifier script (18/28/18 matched) and the MAC-drift frozen-hash check. Merged without a separate approval round per tonight's updated agreement. Handed Gate 5 to Codex as the next continuous task.
- 2026-07-19 Claude: user authorized continuous autonomous work overnight — merges/next-task selection no longer need per-step approval (only genuine business/scope tradeoffs and pushing to GitHub do; see `docs/COLLABORATION.md` Section I rule 4 reinforcement). Scoped Gate 5 from direct investigation (spec placeholder had no detail): read `app/pos/actions.ts`/`lib/order-cart.ts` directly and found `submitOrderV2` has no server-side idempotency key despite its underlying write already being atomic — a real double-order risk on an ordinary network retry, not a contrived edge case. Confirmed the "offline" half of Gate 5's original name is explicitly out of scope per existing owner decision D2. Authored `docs/handoffs/2026-07-19-codex-gate5-pos-checkout-idempotency.md`, sequenced after REV-1, with instructions to proceed through both without a review checkpoint in between.
- 2026-07-19 Claude: owner picked REV-1 (cross-check 2 pre-rule script fixes) as next priority. Authored `docs/handoffs/2026-07-19-codex-rev1-script-fix-crosscheck.md`, pointing Codex at the exact diffs (`b5170da`'s hardcoded-date fix, `24a57bd`'s reference-detection regex fix) and what to verify. Both scripts are read-only reporting tools with no production-data write path either way.
- 2026-07-19 Claude: reviewed and approved Gate 3 Phase B (commit `58aa46a`, migration `0022`). Independently reran the live audit script myself: 0 tables now have `anon`/`authenticated` grants (down from 28), `next_order_num` absent, `rls_auto_enable`'s definition hash byte-identical before/after. Independently queried Realtime publications, `pg_cron` jobs, and webhook-style triggers to verify the "no dependency" claim directly. Reran the suite (498/498), TypeScript (clean), and the MAC-drift-baseline frozen-hash check (still passes). Moved to `COMPLETED.md`. Branch not yet merged — separate decision.
- 2026-07-19 Claude: owner picked Gate 3 Phase B cleanup (`G3-A4`/`A5`/`A6`/`A8`) as next priority. Independently re-verified all 4 items live against the actual database before scoping (unchanged since the original Gate 3 audit; confirmed Gate 4 Phase B's 6 new RPCs stayed service-role-only in the same rerun). Authored `docs/handoffs/2026-07-19-codex-gate3-phase-b-database-hardening.md`. Logged `G3-A7` (RPC internal-caller-check gap) directly in `docs/ACCESS-MODEL.md` requirement #5 as an accepted design tradeoff rather than a task — a technical judgment call, not escalated (server-only architecture, grant boundary is the intentional design).
- 2026-07-19 Claude: reviewed and approved FIX-1 + FIX-2 (commit `fe04f4a`). Independently reran the suite (494/494), TypeScript (clean), and a production build (confirmed `/admin/backup` gone from the route table). Confirmed the bcrypt salt-rounds constant (10) matches existing usage rather than being newly invented, and confirmed the new test file explicitly proves the legacy Sheets code path is never invoked. Moved to `COMPLETED.md`.
- 2026-07-19 Claude: scoped FIX-1 (password change) + FIX-2 (remove Backup&Sync page) as one handoff after owner picked this as the next priority post-Gate-4. Investigated FIX-2 further before scoping: found the manual backup button can never actually create a Drive backup (the server has no Drive-write credentials; `backup-to-drive`'s handler only returns JSON, Apps Script does the actual Drive write under the owner's own Google account) — a materially bigger finding than the original roadmap note ("calls the wrong function"). Presented the owner with options; owner chose full page removal over relabeling/repurposing. Confirmed `sync_state` (migration `0003`) is still live-used by the current backup system and must not be touched. Flagged the now-orphaned `backup-to-sheets` Edge Function for a separate future removal decision rather than bundling an infrastructure undeploy into this fix.
- 2026-07-19 Claude: removed `G4-B1` through `G4-B5` from the P2 backlog table — all 5 are now fixed and merged (see the Gate 4 Phase B entry in `COMPLETED.md`), leaving them described as still-broken would be misleading.
- 2026-07-19 Claude: owner approved merging `codex/gate4-phase-b-atomic-rpcs` into `main`. All 5 Gate 4 Phase B paths verified and closed; see `COMPLETED.md`. Not pushed to `origin` yet.
- 2026-07-19 Claude: reviewed and closed the remaining Gate 4 Phase B paths 3-5 (commits `576572b`/`22823ce`/`016bed6`, migrations `0019`-`0021`). Read all 3 migrations in full; confirmed the decisive `saveProduct` price/price-history atomicity fix at the SQL level (both writes now in one transaction block), confirmed `approve_stock_adjustment_atomic` handles both the always-APPROVED and PENDING-then-approved entry paths per handoff 3a, confirmed `supersedeOrderV2`'s new RPC re-validates the optimistic lock and all cross-references inside the transaction. Independently reran everything: 491/491 tests, TS clean, migrations 0001-0021 local/remote matched, a live read-only product-catalog integrity check (0 orphan variants/history/recipes, 0 multi-active-recipes across 42 real products), live current-stock audit (still exactly the same 3 known negatives), live MAC-drift-baseline audit (frozen-baseline SHA-256 assertion didn't throw), live void-order and stock-adjustment audits (both clean, `SUPERSEDED` order data undisturbed). Confirmed no push/merge occurred. All 5 Gate 4 Phase B paths are now closed technically; merging the branch into `main` is a separate, not-yet-made decision.
- 2026-07-19 Claude: reviewed Gate 4 Phase B path 2 (commit `31cee7f` on branch `codex/gate4-phase-b-atomic-rpcs`). Independently reran everything on the branch worktree: 483/483 tests, TS clean, migration `0018` applied local+remote, live `audit-production-stock.ts` rerun (5 yield mismatches confirmed as the same pre-existing `HISTORICAL/SEMANTIC GAP` from the Phase A baseline, not new drift), live `audit-mac-drift-baseline.ts` rerun completed without the script's own frozen-baseline-hash assertion throwing (that script hard-fails on any SHA-256 mismatch, so a clean run is itself confirmation of the "frozen baseline unchanged" claim). Approved and closed path 2. Resolved Codex's path-3 stop-gate the same way as path 2's: verified live `stock_adjustments` schema directly (missing exactly the 5 columns Codex named, 0 rows, gap traces to `0001_init_schema.sql` itself). Approved migration `0019` (5 columns + 2 atomic RPCs) with 1 addition (handle both the always-`APPROVED` and `PENDING`-then-approved entry paths). Flagged the schema-completeness gap has now shown up twice in one afternoon — worth checking the remaining 2 paths' live schemas early.
- 2026-07-19 Claude: reviewed Gate 4 Phase B path 1 (commit `c6c61b7` on branch `codex/gate4-phase-b-atomic-rpcs`). Independently checked out the branch's own worktree and reran everything: 480/480 tests, TS clean, `npx supabase migration list` confirms migration `0017` applied both local and remote, live `audit-void-orders.ts` rerun shows 0 reversal/event mismatches across 11 live VOIDED orders. Approved and closed path 1. Resolved Codex's path-2 stop-gate: verified the live `production_orders`/`production_items` schema directly (0 rows in either table — the current `saveProductionOrder` write path has never completed a live insert, its column names don't exist on the live tables) and confirmed `types/db.ts` and `ProductionForm.tsx` already assume the canonical shape Codex proposed. Approved the canonical mapping with 3 additions written into the handoff (naming, `audit-production-stock.ts` rewrite, `status='COMPLETED'` at creation). Decided as a technical correctness call, not escalated — no legacy data or user-visible tradeoff exists to weigh.
- 2026-07-19 Claude: owner decided to fix all 5 Gate 4 `needs-atomic-rpc` paths in one combined Phase B effort rather than a partial P1-only pass. Authored `docs/handoffs/2026-07-19-codex-gate4-phase-b-atomic-rpc-remediation.md`: one atomic RPC per path (5 migrations, 5 commits, following the existing `create_pos_order_atomic`/`save_purchase_order_atomic` house style read directly from `supabase/migrations/0006_atomic_purchase_order_write.sql`), idempotency-key guidance per path derived from each path's specific Phase A failure evidence, existing `requireAdmin()` authorization boundary explicitly preserved (not touched by this fix), and per-path stop-and-ping triggers. Moved P1 to this new task.
- 2026-07-19 Claude: reviewed and closed Gate 4 Phase A (commit `159b7c9`, final 2 Item-2 paths + final classification report). Independently reran both new test files (11/11) and the full suite (474/474, up from 463), and cross-checked the report's write-order claims directly against `saveProduct`/`submitStockAdjustment`/`approveStockAdjustment` source. Confirmed `saveProduct`'s edit path can permanently and silently lose price-change audit history (a successful price update followed by a failed history insert leaves no trace for retry to repair) — worse in kind than the other paths' stuck/duplicate patterns. Confirmed stock-adjustment approve can leave a durable "approved but no ledger effect" stuck state. All 5 Item-2 paths (`voidOrderV2`, `supersedeOrderV2`, `saveProductionOrder`, `saveProduct`, stock adjustment) are now finally classified `needs-atomic-rpc`. Logged `G4-B4`/`G4-B5`, finalized `G4-B3` from preliminary. Moved the P1 entry to `COMPLETED.md`. Phase B (actual remediation) is explicitly not authorized by this review — it is a scope/investment decision for the owner now that the full 5-path picture is in.
- 2026-07-19 Claude: reviewed the production/order-edit forced-failure stop-gate (commit `26b2eb8`). Independently reran both new test files and the full suite (463/463). Confirmed `saveProductionOrder` is worse than `voidOrderV2` — no cleanup, no idempotency guard, a failed yield-insert retry silently creates a new production order and double-consumes ingredients. Confirmed `supersedeOrderV2`'s single-failure cleanup works correctly in all 5 tested positions, but a failed cleanup leaves orphan lines that block all future retries. Logged `G4-B2` and preliminary `G4-B3`. 3/3 tested paths are now `needs-atomic-rpc` — pattern looks systemic. Approved continuing to the final 2 paths (`saveProduct`, stock adjustment) before scoping Phase B.
- 2026-07-19 Claude: reviewed `voidOrderV2`'s forced-failure stop-gate (commit `15e3889`). Independently reran the new test and the full suite (453/453) to confirm a real gap broader than anticipated: reversal succeeds + event insert fails → retry silently double-writes the stock reversal. Decided to continue evidence collection on the remaining 4 Item-2 paths rather than open remediation immediately, reasoning that `supersedeOrderV2` (order edit) likely has the same or a worse gap given a similar-but-less-guarded write pattern, so one coherent Phase B atomic-RPC pass is better than fixing paths one at a time. Logged as `G4-B1`, P1, in the P2 backlog table with full evidence.
- 2026-07-19 Claude: reviewed and cleared Gate 4 Item 1 + 1a (commit `c0be7ce`). Read both new audit reports, hand-verified the 12-line MAC classification table (pre-visible replay equals stored COGS in all 12 rows), and independently reran `audit-current-stock.ts` myself to confirm the new `ING-003` finding exactly (-131 g Sữa đặc). Decided `ING-003` is the existing negative-stock-recovery category (needs owner physical count) rather than a Gate-4-blocking issue, and updated that out-of-scope entry to current live state (3 negative: `ING-003` new, `ING-021`/`ING-024` existing; previously tracked `ING-001`/`NNL-003`/`NNL-006` no longer appear live — flagged as unverified-resolved, not confirmed clean). Cleared Codex to continue Item 2.
- 2026-07-19 Claude: owner moved franchise management from item 6 to last (item 7, after security hardening) — not yet certain franchise gets built, so it shouldn't hold up or need to be designed around by anything before it. Security hardening (now item 6) is scoped to the system's shape through multi-branch only; franchise would need its own follow-up security review later if approved.
- 2026-07-19 Claude: 2 owner decisions handled. (1) File/folder organization: added `docs/FILE-ORGANIZATION.md` (naming/placement rule, effective immediately for new files) and inserted "Repository file/folder reorganization" as Future direction item 2 — right after the audit finishes, before feature-completeness, per owner priority. The rule doesn't move any existing file (D8 preservation stands); the actual reorganization pass is proposed-then-approved later, same pattern as Pre-Audit B. (2) Gate 4 stop-gate: Codex's fresh `audit-mac-drift-baseline.ts` rerun found 12 new `NEW_INVESTIGATION_NEEDED` lines (net ~+10 VND, 0 `LOCKED_VIOLATION_STORED`). Decided not to block the rest of Gate 4 Phase A, but added a classification addendum (Item 1a) to the handoff reusing the established Task 3.4 methodology rather than accepting or locking anything without evidence.
- 2026-07-19 Claude: scoped and authored Gate 4 Phase A handoff (order/inventory/COGS business audit). Read `lib/sheets-db-v2-edit.ts` directly and confirmed its own header self-documents `supersedeOrderV2` (order edit) as "Not a true transaction." Traced `voidOrderV2`'s write order and idempotency guard by hand and found a specific, previously-undocumented gap: if the VOIDED event insert succeeds but the following order-status update fails, a retry is incorrectly rejected (idempotency guard sees the event) while the order status still reads COMPLETED with reversed stock already recorded. Confirmed no forced-failure test exists for this or 4 similar sequential-write paths (`saveProductionOrder`, `saveProduct`, stock adjustment submit/approve) — only the already-atomic POS/purchase-order RPCs have one (`scripts/probe-pos-order-rollback.ts`). Scoped Phase A as fresh-audit-rerun plus mocked forced-failure testing (not live probes, to avoid orphaning real data on a non-atomic path) with a 3-way classification per path; explicitly deferred any new atomic RPC or fix to a separately reviewed Phase B.
- 2026-07-19 Claude: Gate 3 Phase A reviewed and closed (commit `a17b0e7`). Independently re-ran the actual live-database audit script from scratch (not just read the report) using the repo's own Supabase credentials — got byte-identical results for every summary statistic and individually confirmed all 10 named RPCs' grant state. This is the strongest verification done in this audit program so far: reproducing live evidence directly rather than trusting an artifact. Also reran the suite (445/445), TypeScript (clean), and production build (success) independently. Logged 5 Phase B findings (G3-A4 through G3-A8) as P2 backlog — Low/Medium severity, no evidence of exploitation, live DB changes need their own reviewed scope.
- 2026-07-19 Codex: implemented Gate 3 Phase A as a read-only live database/RPC/RLS audit. Confirmed 32/32 public tables have RLS enabled with zero policies, the 32-table backup allowlist matches live exactly, ten live application RPCs are service-role-only, and `exec_sql` is absent. A fresh production build placed no Supabase key name/value in 96 browser-static files. Flagged broad dormant grants, orphaned `next_order_num`, and the untracked `rls_auto_enable` event trigger as separately reviewed Phase B inputs; no live configuration changed. Awaiting Claude review.
- 2026-07-19 Claude: scoped and authored Gate 3 Phase A handoff (read-only database/RPC/RLS audit). Investigated the repo directly before writing it rather than guessing from the thin spec: found `lib/supabase.ts` documents an intended anon-key+RLS browser client that was never built (0 matches for `NEXT_PUBLIC_SUPABASE`/browser-client patterns anywhere), found 0 of 16 tracked migrations enable RLS or define any policy, and found `scripts/check-constraint-query.ts` calling an `exec_sql` RPC (raw SQL execution) via the service-role client — confirming that function likely exists live, with its actual grant to `anon`/`authenticated` unknown and flagged as the single highest-priority item in the gate. Scoped Phase A as evidence-only (live RLS status, role grants, RPC security-definer review); explicitly deferred any RLS/grant remediation to a separately reviewed Phase B given live database configuration changes carry real availability risk.
- 2026-07-19 Claude: Gate 2 Remediation Wave 2 reviewed and closed (commits `a20aba8`, `79bda17`, `71b319f`) — **Gate 2 fully closed** (map + both waves). Read every diff line: confirmed `getPOSBestSellerProductIds` returns only a plain product-ID array (test asserts every value is a string, standalone toppings excluded) despite reusing `breakdownRevenueByProduct` internally; confirmed `getPOSStockStatus` returns only `{id, current_stock}` with no cost/supplier fields; confirmed `app/pos/page.tsx`'s swap preserves identical `bestSellers`/`stockMap` output (data-source change only). Independently reran the suite (438/438), TypeScript (clean), and the access-audit tool itself (83 actions, 100% `GUARDED`, 0 findings). Gate 3 (database/RPC/RLS) not yet scoped.
- 2026-07-18 Claude: approved Codex's Wave 2 stop-gate proposal. Codex correctly stopped rather than blindly applying `requireAdmin()` to `getRealtimeStock`/`getSalesDataV2` (would have broken STAFF checkout, since `app/pos/page.tsx` calls both and STAFF can reach `/pos`). Checked exactly what the POS page consumes from each before approving, so the amended handoff specifies the minimal shape precisely rather than leaving it to guesswork: best-seller product IDs only (not the full sales report), `{id, current_stock}` pairs only (not full inventory detail). Amended `docs/handoffs/2026-07-18-codex-gate2-remediation-wave2-admin-reads.md` in place before Codex resumed.
- 2026-07-18 Claude: Gate 2 Remediation Wave 1 reviewed and closed (commits `5c4a01b`, `a1ace53`). Read every diff line: `resolveActor()` correctly rejects unauthenticated POS calls while keeping CLI_MODE's env-controlled (not client-spoofable) SYSTEM path and still accepting any authenticated role (STAFF preserved for POS); `submitStockAdjustment` now `requireAdmin()`-gated per the owner's policy decision, dead PENDING branching removed; the Edge Function's JWT-payload-decode replaced with a constant-time comparison against the real runtime service key, matching the pattern already used by `backup-to-drive`. Read all 3 new test files (rejection-before-mutation, STAFF-rejected/ADMIN-succeeds, forged-JWT-rejected/real-key-accepted). Independently reran the suite (430/430), TypeScript (clean), and the access-audit tool itself (0 mutation findings, 61 guarded, 20 `UNGUARDED_READ` unchanged — exact Wave 2 scope). Wave 2 unblocked for pickup.
- 2026-07-18 Claude: reviewed Antigravity's UI-REMED-1 visual smoke test commit (`2cabde9`) independently rather than trusting the self-report — read all 10 file diffs (Alert/Badge/Button/LoadingButton primitives, POSScreen toasts/modals, login/settings-password error states, ModifierForm, ActivityLogClient, SemiProductsClient), confirmed every change is a semantically-equivalent raw-color-to-token swap with no logic change, including a genuine typo catch (`bg-primary-soft0`, an invalid class, silently rendering no background). Independently reran the suite (422/422) and production build (success) rather than trusting the claims. Approved and moved to `COMPLETED.md`.
- 2026-07-18 Claude: owner resolved SEC-5 — `submitStockAdjustment` is being locked to ADMIN (stock adjustment is manager/admin responsibility, not staff). Folded into Wave 1 handoff before Codex picked it up, removed from Blocked. Also corrected Antigravity's own "(verified)" self-report on the UI-REMED-1 visual smoke test to `[!]` pending Claude review — a self-report isn't a completed second-party review, same principle as the scripts/ self-review rule.
- 2026-07-18 Claude: Gate 2 reviewed and closed (commits `3570da0`, `f14b092`). Independently reran the suite (422/422) and TypeScript (clean), then directly read source for the 4 highest-risk claims before trusting them — confirmed the POS SYSTEM-actor fallback, the zero-guard `deletePOSDraft`/`getPOSDrafts`, the `submitStockAdjustment` PENDING-vs-approved distinction, and the Edge Function's unsigned service-role JWT check all hold up exactly as reported. Split the 25 findings into 2 scoped remediation waves (P0: POS + Edge Function signature; P1: 20 admin-read guards) rather than one big unreviewed fix. Flagged `submitStockAdjustment`'s policy as a genuine business decision (changes who can currently do what, not just security posture) rather than letting Codex decide unilaterally. Flagged Edge Function deployment-config verification as needing dashboard access, not a code task.
- 2026-07-18 Claude: found genuine ready work for idle Antigravity (user asked directly) rather than inventing busywork — UI-REMED-1's visual smoke test was closed on automated checks only and never actually looked at in a browser. Authored handoff, added as P2, cleaned up the stale "pending prompts" list (Pre-Audit C was listed as still needing pickup despite being closed weeks-equivalent ago in session time; added Gate 1/Gate 2 entries).
- 2026-07-18 Claude: scoped and authored Gate 2 handoff. Read `lib/admin-auth-guard-audit.ts` directly and found 3 concrete blind spots in the existing audit tool before writing the handoff: it only scans `app/admin/` (misses `app/pos/actions.ts` and `app/actions/auth.ts`), its mutation-name prefix list misses void/reject/create/change-named functions (this is exactly why `rejectEventAction`'s Gate 1 gap wasn't caught by the tool itself), and its guard check only tests for substring presence, not enforcement, and misses arrow-function exports entirely. Gate 2 scope: fix the tool, extend it to API routes, produce a dated evidence report, remediate small unambiguous findings, cap silent remediation at 5 items before requiring a stop-and-report.
- 2026-07-18 Claude: Gate 1 reviewed and closed (commits `dd2f970`, `57d298a`, `9a8ee66`). Independently reread every diff line, confirmed the remaining `select('*')` never leaks to a response, read all 5 new regression tests, and reran the suite (414/414) and TypeScript (clean) rather than trusting the report. Moved to `COMPLETED.md`. Opened Gate 2 (architecture/access map) as P1 — scoped pragmatically since the source spec's own Gate 2/Phase 3 text is incomplete.
- 2026-07-17 Claude: owner triggered the full eight-gate audit program after reviewing Pre-Audit C findings. Froze baseline `24a57bd`, updated the program spec status to ACTIVE, folded SEC-1/SEC-2/SEC-3 into a Gate 1 handoff (`docs/handoffs/2026-07-17-codex-gate1-p0-security-exposures.md`) since they are security exposures; kept FIX-1/FIX-2 as separate P2 functional-bug backlog. Gate 1 open for Codex, P0.
- 2026-07-17 Claude: closed Pre-Audit C review (commit `99f466d`). Independently re-derived the 4 most consequential findings from source code rather than trusting the write-up: broken password-change feature, unguarded backdated-ledger approval action, 2 unauthenticated maintenance routes, manual backup button calling the legacy path. Precise per-row status count matches claim exactly (15/18/14/3/0/1 = 51). Added 5 concrete P2 backlog items with evidence. Opened "next audit stage" as a Blocked/owner-decision item — F&B 17-section checklist vs eight-gate audit trigger.
- 2026-07-17 Claude: closed Pre-Audit B execution review. Independently reproduced all claims (10/10 canonical docs, 64/64 links, 403/403 tests, TS clean, build clean 41 routes, 7 SUPERSEDED + 1 DUPLICATE banners spot-checked). Authored Pre-Audit C handoff (`docs/handoffs/2026-07-17-codex-pre-audit-c-feature-inventory.md`), scoped to the 15-module FEATURE-CATALOG population pass; deferred the full F&B capability checklist as a separate follow-up. P1 unblocked for Codex pickup.
- 2026-07-17 Codex: Pre-Audit B execution created/refreshed the ten canonical entry documents, preserved Tier 2/3 sources, and moved Pre-Audit C to the next P1 gate. Pending Claude review; no push.
- 2026-07-17 Claude: UI-REMED-1 saga closed. All 5 phases done (commits `c33033f` + `8f93742` + `d239cbb` + `55ef69d` + `ee33450`). ~94% color migration coverage (145 raw occurrences remain — ui/* primitives + gradient stops + complex utility classes). TS clean, build clean, 403/403 tests pass. Visual smoke test pending. UI-REMED saga 5/5 + 1 polish complete. ROADMAP cleaned up duplicate P1 sections.
- 2026-07-17 Claude: UI-REMED-1 async overnight brief authored. User sleeping, Antigravity authorized to run 5-phase TOKEN-SWAP migration overnight. NO PUSH rule. Final report to `docs/reports/ui-remed-1-overnight-report.md` for morning Claude review.
- 2026-07-17 Claude: UI-REMED-5 closed (commit `11c566b`, reviewed). Button warning variant + Dialog icons by variant. UI-REMED saga 4/5 + 1 polish complete. ROADMAP cleaned up duplicate P1 sections. Only UI-REMED-1 TOKEN-SWAP remaining (P2, largest, multi-session).
- 2026-07-17 Claude: UI-REMED-4 closed (commit `c923086`, reviewed). Root error/loading boundaries + 5 missing loading.tsx. UI-REMED saga 4/5 done. P1 cleared. Last: UI-REMED-1 TOKEN-SWAP.
- 2026-07-17 Claude: UI-REMED-3 Session 2 closed (commit `2f91b3f`, reviewed). All 52 native alert/confirm migrated to Dialog API. Independent grep confirms 0 remaining. UI-REMED-3 saga complete (Sessions 1+2). P1 cleared.
- 2026-07-17 Claude: UI-REMED-3 Session 1 closed (commit `dd51dae`, reviewed). Dialog API + components + proof-of-concept migration done. Opened Session 2 (bulk migrate 53 call sites across 18 files) as new P1.
- 2026-07-17 Claude: UI-REMED-3 split into 2 sessions. Authored Session 1 handoff (Dialog primitive + lib/dialog imperative API + DialogHost + proof-of-concept migration). Session 2 (bulk migration 53 call sites) deferred to next handoff.
- 2026-07-16 Claude: UI-REMED-2 closed (commit `6b65aba`, reviewed). StickyFilterBar redesigned with design tokens, API + sticky + mobile expand preserved. 16 clients auto-inherit. P1 cleared → next UI-REMED-3 REPLACE-ALERT.
- 2026-07-16 Claude: Task 3.10 closed (commit `6a5bdec`, reviewed). Audit OPERATIONALLY CLEAN exit 0. MAC drift saga complete (E3 → Task 3.10). P1 cleared.
- 2026-07-16 Claude: User + Codex chose Task 3.10 Option B (accept informational, no DB write) + improve audit display. Authored handoff brief. Small scope ~30 min Codex.
- 2026-07-16 Claude: Task 3.5 closed (commit `c28319d`, reviewed). 4-bucket classifier + sub-classification for LOCKED_VIOLATION. 16 LOCKED_VIOLATION_REPLAY surfaced = E3 baseline lines also affected by BTP drift. Opened Task 3.10 (P1, user decision required: re-classify vs accept).
- 2026-07-16 Claude: User picked up Task 3.5 (cohort-aware MAC drift baseline audit) after Phase 3 push. Authored handoff brief. Promoted from P3 to P1. Other Task 3.5 P3 items remain deprioritized (V1 wait-for-event, UI-CONSISTENCY-1).
- 2026-07-16 Claude: Stabilization Phase 3 closed. Build gate passed, 2 close-out commits (`86f2b89` + `3a55939`), fast-forward push to `origin/main` (HEAD now `3a55939`). 50+ commits live on GitHub. Vercel auto-deploys. Stabilization saga complete.
- 2026-07-16 Claude: Stabilization Phase 2 closed (commits `98557ed` + `0fb8f9d` + `9dddc4a`, reviewed). Production verified: Apps Script pull-model, 32 tables, daily+monthly retention, file xuất hiện trong Drive. Backup ownership added to COLLABORATION.md Section C (Codex owns backup architecture). P1 cleared → opened Stabilization Phase 3 (push 70+ commits) as new P1.
- 2026-07-16 Claude: Task 3.9 closed (commit `09bf26a`, reviewed). MAC drift audit fully clean (436 locks). Phase 1 UI audit closed (commit `cdc8d56`, reviewed). 1279 issues → 4 post-push remediation backlog items (UI-REMED-1 to 4). P1 cleared → opened Stabilization Phase 2 (Drive backup) as new P1.
- 2026-07-16 Claude: Task 3.8 closed (commit `ad7f7ba`, reviewed). 41 lines map to 5 historical ledger rows, 0 durable events (migration 0014 gap). User chose Option A (accept as drift). Authored Task 3.9 handoff brief for lock cohort.
- 2026-07-16 Claude: User chose walk-through approach A (Codex surface first). Authored Task 3.8 handoff brief for read-only investigation: map 41 BACKDATED_LEDGER_LIKE line IDs to `backdated_ledger_events` + status breakdown. Old P2 "Task 3.2 review path" row removed (subsumed by Task 3.8 in P1).
- 2026-07-16 Claude: Task 3.7 final review approved (commit `d2177ca`). 170 → 395 locks, 225/225 cost unchanged, idempotent rerun `ALREADY_APPLIED`. Moved to `COMPLETED.md`; P1 cleared. MAC drift audit clean except 41 BACKDATED_LEDGER_LIKE (Task 3.2 path).
- 2026-07-16 Claude: User chose Option B (accept + lock). Authored policy doc `docs/audits/2026-07-16-btp-recipe-replay-drift-policy.md` and handoff brief `docs/handoffs/2026-07-16-codex-task-3.7-btp-drift-lock.md`. Task 3.7 marked `[~X]`, ready for Codex pickup.
- 2026-07-15 Claude: Task 3.6 closed (commit `d32d4d4`, Claude reviewed). Root cause: nested BTP recipe snapshot not pinned in audit replay; stored COGS correct at sale time. Opened Task 3.7 as P1 decision task (user picks remediation path A/B/C).
- 2026-07-15 Claude: Task 3.4 closed (commit `fea097d`, Claude reviewed). Moved to `COMPLETED.md`. Opened Task 3.6 forward-drift investigation as new P1. Added 2 backlog items: 41 BACKDATED_LEDGER_LIKE review path, 112 historical drift acceptance decision.
- 2026-07-15 Claude: authored Task 3.4 read-only handoff brief (`docs/handoffs/2026-07-15-codex-task-3.4-outside-cohort-investigation.md`). P1 ready for Codex pickup. Marked `[~X]` in priority queue.
- 2026-07-15 Claude: E3 final review complete. Six cohort gates pass, audit trail intact (snapshot/source/run IDs), rollback procedure documented. E3 moved to `COMPLETED.md`; P0 cleared.
- 2026-07-13 Codex: closed E3 after atomic 40-line recovery and added Task 3.4/3.5 follow-ups.
- 2026-07-10 Claude: created as single source of truth. Superseded `docs/handoffs/2026-07-09-codex-roadmap.md` and `docs/handoffs/2026-07-06-antigravity-roadmap.md` (both deleted).
