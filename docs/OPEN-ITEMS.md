# Open Items

The single list of what is not finished. Kept short on purpose — one line of
what, one of why it is still open. Detail lives in the linked plan or spec.

Updated 2026-07-31. Update it in the same commit that changes an item's state;
if it only exists in a chat message, it does not exist.

**Why this file exists:** the owner asked twice what remained, and both times the
answer had to be reconstructed by counting, because the plan files' checkboxes
were never ticked and the roadmap only tracks closed phases. See "Tracking debt"
at the bottom.

---

## Backup

| # | Item | Why open |
|---|---|---|
| 3 | ~~**The daily bundle will exceed 50 MB again**~~ **NO LONGER URGENT — collapsed 2026-08-08** | Was **40.8 MB** on the 2026-08-07 bundle with 9.2 MB of headroom. The owner's Drive listing on 2026-08-17 shows **38.9 MB on 07/08 and 6.2 MB on 08/08** — an 84% drop overnight, and **6.7 MB today**. Cause measured, not inferred: `stock_ledger` is now **194 rows** (was 11.700+) because Plan C Task 5 deleted every derived row on 2026-08-07, the night between those two bundles; and `data_recovery_changes` is **0 rows** (was 46k / 15.88 MB) because Task 6 retired the machinery that wrote to it, so retention had nothing left to hold. **Growth is now ~57 KB/day** (6.2 → 6.7 MB over 9 days), putting 50 MB roughly **two years** away at current trading volume. What remains is almost entirely real sales data: `order_lines_v2` ~3.0 MB, `orders_v2` ~1.9 MB, `order_events` ~1.2 MB = 6.1 of 6.3 MB. Keep the item open only as a thing to re-check yearly, or when trading volume changes materially. Plan (now unneeded): `docs/superpowers/plans/2026-07-31-split-recovery-log-from-backup.md`. |
| 4 | ~~gzip the bundle~~ **moot for now** | Was deferred pending a test of whether Apps Script's 50 MB limit applies to compressed bytes. With the bundle at 6.7 MB (item 3), there is nothing to compress toward. Do not spend the test on it unless item 3 changes. |

## Logging hygiene (analysis done 2026-07-31, nothing implemented)

Measured: `data_recovery_changes` is **60.4%** of all stored data (15.88 MB,
46k rows). The business logs are tiny — `order_events` is 3.8%, and only **28 of
its 1,844 rows** carry information not derivable from the order itself.

| # | Item | Why open |
|---|---|---|
| 5 | Stop writing `CREATED` order events | 1,061 rows duplicating what `orders_v2` already records. |
| 6 | Delete 755 `MIGRATED` order events | One-time V1→V2 artifact, spent. |
| 7 | **Keep** `VOIDED` (15) / `EDITED` (13) / `purchase_order_edits` (2) | Not a task — a decision recorded so nobody "optimises" them away. `VOIDED` rows carry `net_total_before` and a typed reason: the only record of money leaving the books, unreconstructable afterwards. `purchase_order_edits` records subtotal and line-count before/after, which is exactly what would have caught PO-037. |

**Re-measured 2026-08-17:** the figures above are dead. `data_recovery_changes`
is now **0 rows**, not 60.4% of stored data — the machinery that wrote it was
retired (Plan C Task 6). `order_events` is 2.187 rows, ~1.2 MB, of which items
5 and 6 would remove ~1.816. **Their original justification was the 50 MB
backup ceiling, which no longer applies** (item 3): the whole bundle is 6.7 MB.
Items 5 and 6 are now housekeeping, not risk mitigation — reprice them
accordingly before anyone spends a session on them.

**Rule for future features:** log it only if it (a) changes money or stock **and**
(b) is not derivable from the record itself. Void, edit, discount, price change,
PO edit, stocktake adjustment → yes. Create, view, search, login → no.

## Costs that never reach a report

| # | Item | Why open |
|---|---|---|
| 8 | **Operating-expense entry screen does not exist** | The owner chose to treat ice, limes, kumquats and sweet potato as daily expenses rather than tracked stock. With nowhere to record them, that cost currently vanishes from every report, and any drink using them shows a higher margin than it earns. |
| 9 | Khoai lang wired half-way | Owner parked it deliberately. See spec section 7a — `RC-032` exists, but nothing consumes `BTP-014`, `REC-069` is empty, and `NNL-012` is still non-inventory. Do not read `RC-032` as evidence it is costed. |
| 10 | Sữa đặc: no purchase recorded since 2026-05-16 | Either never entered or entered against a different purchased item. Spec section 7b. |

## Phase 7 and later

| # | Item | Why open |
|---|---|---|
| 11 | 17 forms, 11 competing input styles | The owner's own stated priority; nothing built yet. |
| 12 | ~~Backfill `start_date` on 129 recipes~~ **DONE 31/07** | 124 rows backfilled (`7364ffe`), `NOT NULL` added (`0048`), read-time fallback removed (`0049`, `acf2a68`). Equivalence proven across 4,820 replayed selections. Side effect was 132 spurious `backdated_recipe_events` rows -- moot, that machinery retired entirely 2026-08-06 (Plan C Task 6). |
| 12b | **Task 5 never ran, and the path it audits now has live data** | Task 5 measures whether `findLatestActiveRecipe` (sorts by `created_at`, ignores effectiveness) and the `end_date` close-out handle future-dated recipes. `RC-035` and `RC-038` now carry `start_date` 2026-08-31. Plan: same file, Task 5. |
| 13 | **Load speed: the data layer is now measured, the rest is not** | One of the owner's four original priorities. **Measured 2026-08-17 (POS):** the five full-table fetches run in parallel at **~110 ms warm / 1.288 ms cold**, the best-sellers query at **~250 ms warm / 868 ms cold**, and the page ships **26,6 KB** of catalogue data. **Data is not the bottleneck**, and the cold/warm ratio of roughly 10x is worth knowing — a till left idle pays the cold price. **Still unmeasured, and it needs a real browser:** server render time (`/pos` is `force-dynamic`, so nothing is cached), browser startup for the **118 kB** first-load bundle, and every in-screen interaction. Owner deferred that to Plan I's final assessment (`docs/superpowers/plans/2026-08-17-final-evaluation.md` §4), where it belongs with the question of whether the refactors helped. |
| 45 | **Final assessment of Plans C-H has not been done** | Owner asked 2026-08-17 whether the POS split actually made things better, whether more reduction is possible, and whether the same treatment generalises to other files — and set it as the **last** step after the backlog clears. Plan: `docs/superpowers/plans/2026-08-17-final-evaluation.md`. The one thing it must not blur: **the split did not make the POS faster** — the `/pos` bundle is unchanged at 21,8 kB / 118 kB, and total lines went up (242 removed, 327 added). What changed is that a change to item configuration now touches 258 lines with 14 tests instead of 1.378 lines with none. |
| 14 | Retire `data_migration_runs`, dead config rows (`BTP-004`) | Retire by marking inactive — never delete master data. |
| 14b | Test semi-products sitting in the live catalogue | "Test lần 2" (`BTP-016`) and "Test Task6 Step8" (`BTP-017`) are `ACTIVE` in real master data, from the 31/07 live verification steps, along with recipes `RC-033`-`RC-040`. "Test" (`BTP-015`) is `DELETED` but its recipe `RC-035` is still `ACTIVE` — the deleted-semi-product shape the start_date plan warns about. Nothing broken (no stock, no orders); retire by marking inactive, ask the owner first. |
| 15 | **Physical stocktake — priority changed 2026-08-07, not a Phase 7 item anymore** | Originally deferred to "the last act before expansion." Plan C's cutover (Task 4/5, 2026-08-07) makes it the switch that turns the cost engine on: `stock_issues` is empty, so every COGS report reads 0đ for every period until the first count closes (`CLAUDE.md` section 7, `BR-COGS-005`). Sequencing this against Phase 7 is the owner's call, not decided here — flagged because the old framing ("last act before expansion") now reads as lower priority than it is. |
| 16 | Shift and cash reconciliation (`FC-3`) | Owner deferred: no staff yet. |
| 37 | **Deactivating or deleting a purchased item's last countable conversion has no guard** | `deleteConversionAction` (`app/admin/inventory/conversions/actions.ts`) marks a referenced conversion `INACTIVE` or hard-deletes an unreferenced one, with no check for whether it is the item's only `ACTIVE`, non-`purchase_only` conversion. Same C17-shaped freezing risk D15's `wouldLeaveNoCountableConversion` guards against for the `purchase_only` flag (`lib/conversion-countability.ts`) — an item left with zero countable conversions can never be counted again, and under S1/S2 its ingredient's quantity freezes permanently. Flagged while building D15 (2026-08-10), not fixed there — the owner asked about the `purchase_only` trap specifically, this is a different code path. |
| 35 | ~~**Revenue has been proved stable, never proved correct**~~ **CLOSED 2026-08-17 by Plan H** | Audited: `docs/superpowers/plans/2026-08-14-revenue-audit.md`, and `scripts/verify-revenue.ts` re-runs every structural check on demand. Result across all completed orders: header arithmetic, line sums, promotion recomputation and the superseded-version exclusion all clean; payments and revenue agree to 0đ from 2026-07-19 onward. Two things it did **not** settle, both recorded elsewhere: 44.229.000đ before 2026-07-19 is permanently unverifiable (`BR-SALE-005`), and the order-edit path lets an administrator set a discount with no promotion behind it (`OPEN-ITEMS 44`). Original text follows. | Owner instruction 2026-08-09: *"sau này khi làm lại các tính năng của đơn hàng và báo cáo anh cũng cần tính rà soát lại xem doanh thu đã thể hiện đúng hay chưa."* Plans C and D used June 22.157.000đ and July 18.661.000đ as a gate on every task — but a gate proves a figure **did not move**, which is not the same as proving it is right. Cost was audited line by line and found 7,4% wrong (`BR-COGS-006`); revenue got no equivalent pass. Specific things never checked: whether `net_total` correctly nets promotions, order-level discounts and modifiers; whether `findCompletedOrders`' filters (`COMPLETED` and no `superseded_by` exclusion — see `BR-SALE-004`) are the intended definition of countable revenue rather than merely the current one; and whether voided/edited orders leave revenue overstated. **Do this before the financial report rebuild (item 31), not after** — a rebuilt report resting on an unaudited revenue figure inherits the error and makes it harder to see. |
| 38 | ~~Plan D's component tests assert against source text, not rendered output~~ **CLOSED 2026-08-17** | Written 2026-08-08 (D6) believing the repo had no jsdom. It does: `jsdom` is in `package.json`, `vitest.config.ts` includes `components/**/*.test.tsx`, and `components/ui/Dialog.test.tsx` / `components/DialogHost.test.tsx` already render for real with `createRoot` + `act`. **Two failure modes, both real:** a source-text assertion passes while the component throws at render (the string is present, the screen is not), and it fails on a rename that changes nothing a user sees. **Rationale corrected 2026-08-17:** previously cited the 2026-08-09 stocktake outage -- wrong, that defect was a server action throwing on an unsupported sort column, no component test could have caught it. The case stands on the two failure modes alone. **Both screens converted 2026-08-17.** `StocktakeClient`: `StocktakeClient.test.tsx` (10 render tests) covers package-size counting and its summed base quantity, per-line independent saving (proven by call count/arguments on a mock, not a function name in the file), BR-INV-007's decimal rejection, the confirmed-badge clearing (C6), legacy-vs-package card selection, tap-target sizing, the counted/total banner, and the preview-gates-apply flow. `StocktakeClient.test.ts` slimmed 12 -> 2 (Tailwind responsive/positioning classes jsdom cannot evaluate). `IssueSlipClient`: classified all 20 assertions before converting, sent for review, corrected twice -- the two-column layout is (b) as drafted, but "compact field sizing" was wrongly grouped with it and is (a); the on-hand/RPC-refusal claim reframed as "does not pre-empt the RPC for this specific over-limit input" rather than left as an absence claim. `IssueSlipClient.test.tsx` grew from 2 tests (the OPEN-ITEMS 41 unit-label pair) to 28, covering package-size counting into the RPC payload, the backdated-month confirm naming the actual months, per-line validation naming the wrong line, the single RPC call carrying all lines of a multi-line slip, reversal eligibility/confirm-message-content/linked-both-ways/per-line-button-count, the explicit empty state, and the class-presence-only checks (tap targets, field width) named as such in their own test titles, not only in a comment. `IssueSlipClient.test.ts` slimmed 20 -> 1 (`lg:grid-cols-2`, breakpoint-conditional, same reasoning as Stocktake's). **Proven to have teeth on both screens, mutations on real logic not cosmetic strings, reverted after:** Stocktake's `sumBaseQty += parsed * pkg.conversionRate` -> `+ 1` failed 2 render tests with the exact wrong numbers (51->53, 48->49) while the old source assertion of that same line still passed. IssueSlipClient's `payloadLines.push({..., baseQuantity: parsedQty * pkg.conversionRate})` -> `+ 1` failed 3 render tests (36->37, 11988->11989, and each line of a 3-line slip off by one) while both old source assertions covering that line (`"parsedQty * pkg.conversionRate"`, the `payloadLines.push`/one-`createIssueSlip`-call checks) still passed -- the substring survives the mutation either way. **Net counts:** Stocktake 12 source-text -> 2 source-text + 10 render (suite total unchanged, composition changed). IssueSlipClient 20 source-text -> 1 source-text + 28 render (suite total 1169 -> 1176, +7: -19 deleted, +26 render added). **Both convertible screens done; item closes.** What is left is not this item: any other component with the same source-text-only shape is unaudited -- this item never claimed to be exhaustive across the codebase, only Plan D's two named screens. |
| 39 | **The POS item modal previews a promo price the cart may never charge** | Two independent promo calculations in `components/POSScreen.tsx` disagree by construction. `promoDetailsMap` (242-298), which drives the modal's price preview *and* the product-tile price, accepts a promo on filters `status=ACTIVE`, `type=PRODUCT_DISCOUNT`, date window, brand — and picks the best promo **per variant**. The cart charges via `appliedPromo` (430-538), which picks **one winner for the whole order** and additionally honours `code` and `min_order_value`. Four ways they diverge: (a) a promo requiring a `code` the cashier never entered still discounts the tile and modal; (b) `min_order_value` is ignored by the preview; (c) an `ORDER_DISCOUNT` promo winning makes `itemPromoDiscounts` (540-578) all zero after the modal promised a per-item discount; (d) with two product promos, per-variant best and whole-order best can select different promos. Also: the cart caps each line at `Math.min(baseTotal, itemDiscount)`, the modal does not. **Current impact: none.** Both promotions in the live table (`PRM-003`, `PRM-004`) are past their window as of 2026-08-13, neither has ever had a `code`, both have `min_order_value` 0, and no `ORDER_DISCOUNT` promo has ever existed — so (a), (b) and (c) have never been reachable in this shop's data. (d) was reachable on exactly one day: `PRM-003` ran to 2026-06-30 and `PRM-004` from 2026-06-30, so both were live that date. **Not checked:** whether any 2026-06-30 order actually contained a variant covered by only one of the two. Found 2026-08-13 while reviewing Plan F's F1 tests; deliberately not fixed inside a refactor (Plan F rule 2). Relevant to item 35 — this is display-vs-charge, so it would show as a price the cashier quoted differing from the recorded total, not as a revenue miscalculation. |
| 40 | **The POS item modal is hand-rolled and traps nothing** | `components/pos/ItemConfigModal.tsx` renders its own backdrop and panel instead of using `components/ui/Dialog.tsx`, which already provides a focus trap, ESC-to-dismiss and click-outside. The consequences: no `role="dialog"`, no focus management, and the product tiles and cart rows behind the backdrop **stay in the tab order** while it is open. The backdrop stops the mouse (`z-50`, opaque, everything behind tops out at `z-40`) but not the keyboard. This is what made the F2 regression reachable (see `DEVELOPMENT-TRACKING.md`, 2026-08-13): a reachability argument based on pointer events alone concluded a path was impossible when it was not. Found 2026-08-13 reviewing F2; not fixed because adopting `components/ui/Dialog.tsx` changes markup and Plan F forbids that inside the split. Affects the drafts modal at `POSScreen.tsx` 1091 too — same hand-rolled shape, and it is F3's subject. **Related, found during F2b (2026-08-14):** the cart-line edit trigger in `components/pos/CartItemRow.tsx:102` is a bare `<div onClick>` with no `tabIndex` and no `role`, so it is not keyboard-reachable at all — which is why only the product tiles (real `<button>`s) could reach the F2b defect. Whoever fixes this item should cover that element too: making it focusable without a focus trap in place would widen the very path F2b's `key` now guards. |
| 41 | **`purchased_items.default_unit_id` is null for all 52 rows** | **Closed for the unit-label symptom, 2026-08-17.** Both screens that read it (`app/admin/inventory/issue-slips/actions.ts`, `app/admin/reports/issued`, G4) source the unit from `UOM_Conversions.base_unit` instead, with no exceptions now: `SPM-043` "Sữa chua không đường Vinamilk" was the one item whose conversion (`QD-049`) disagreed with its own ingredient's unit (`ml` vs `g`); the owner confirmed 100 g per hộp and approved `scripts/correct-qd049-base-unit.ts --apply`, which corrected that one column on that one row (`conversion_rate` and all purchase history untouched, verified before and after: `SPM-043`'s issued quantity, issued value and closing value all identical, every frozen monthly revenue figure unchanged). The `SPM-043` special case in `issue-slips/actions.ts` was then removed — the generic lookup now covers all 52 items. Re-ran the 52-item conversion-vs-ingredient agreement check after the correction: 52/52, zero disagreements (the join itself returns 57 rows, not 52, because 4 items legitimately carry more than one `ACTIVE` conversion — e.g. two package sizes — but each of those is internally consistent and each still agrees with its own ingredient, so this does not reopen anything). Both screens re-verified by rendered test (OPEN-ITEMS 38): `app/admin/inventory/issue-slips/components/IssueSlipClient.test.tsx` and the new `app/admin/reports/issued/ItemCard.test.tsx` (extracted `ItemCard` into its own module because Next's route-module typing forbids extra named exports from `page.tsx`). **Still open:** whether `purchased_items.default_unit_id` should be backfilled at all, or left null with every screen sourcing the unit from `UOM_Conversions` instead — still the owner's call, not resolved by this fix. |
| 42 | **`INACTIVE` hides nothing — 28 filters in the app test only for `DELETED`** | `CLAUDE.md` section 2 forbids deleting master data and prescribes marking it inactive instead. That instruction currently has no visible effect: `app/admin/products/page.tsx:66` filters `baseIngredients` on `status !== "DELETED"` alone, so an `INACTIVE` ingredient still appears in the recipe picker, and `grep -rn 'status !== "DELETED"'` finds **28** such filters across `app/`, `components/` and `lib/`. Found 2026-08-14 when the owner asked why a duplicate "Sữa yến mạch" he had retired was still offered in the Sửa Món dialog. Measured: `base_ingredients` holds 46 rows, 45 `ACTIVE` and exactly one `INACTIVE` (`NNL-004` "Sữa yến mạch", the duplicate of `ING-033`), and **no recipe references `NNL-004`** (0 of 139), so hiding it changes no existing recipe. **The general fix is not a blanket filter:** recipes are versioned with `start_date`/`end_date`, so an older recipe legitimately referencing a since-retired ingredient must still render its name rather than a blank select — the picker has to exclude `INACTIVE` from what is *offered* while keeping whatever the edited recipe already *references*. Fixed for the recipe picker only; the other 27 sites are unreviewed. |
| 43 | **`order_lines_v2.created_at` is not the sale time on migrated rows** | On `ol-migrated-*` lines it records when the V1→V2 migration script ran — one bulk timestamp for thousands of rows — not when the drink was sold. The sale time lives on `orders_v2.created_at`, reached through `order_id`. Found 2026-08-14 during H7b: a check dated PROD-025's sales from the line timestamp and got a range starting 2026-06-28, against 2026-06-03 from the order timestamp. Same data, same product, two different histories, and **neither query errors** — the wrong one just returns a plausible date range. Anything grouping sales by time must join to the order; nothing in the column's name warns you. Not a defect to fix (the migration is history and the column honestly records when the row was written), which is exactly why it needs writing down. |
| 44 | **Editing an order keeps the promo amount but loses which promotion justified it — and lets an administrator set that amount freely** | `lib/order-cart.ts:391-393`: when the caller supplies `item.promo_discount_snapshot`, that value is charged verbatim, clamped only to `[0, gross]`, and `computePromoForLine` (the server's own promotion arithmetic) runs **only as a fallback when nothing is supplied**. Line 420 then writes `promo_discount_reason = "SNAPSHOT"` rather than a promotion id, because `resolvedPromo` is null. **The only caller is `app/admin/orders/OrderEditModal.tsx:185`, the order-edit screen** (`editOrderV2`, `requireAdmin`) — **the POS does not send this field at all**, so a normal sale always goes through the server's own arithmetic. **The snapshot exists for a good reason:** editing a June order in August must not recompute its discount against promotions that have since expired, which would silently strip a discount the customer already received. **Two distinct problems, both real.** (a) *Attribution is lost on edit:* of the 4 orders carrying a discount with no `applied_promotion_id`, three (`UCK000320`, `UCK000407`, `UCK000442`) are version 2 of an edited order whose **version 1 still carries the correct `PRM-003`/`PRM-004`** — the amount survived the edit, the reason did not. The fourth, `PHD000530`, is a migrated V1 order where the attribution never existed. (b) *The amount is unconstrained:* an administrator editing an order can set any per-line promo discount up to the full line value, and it is recorded as an ordinary completed sale. **The money is not wrong today:** all 46.000đ across the four matches a then-live promotion to the dong. **Found by H3 (2026-08-17). Corrected the same day:** this entry first claimed anyone operating the till could do it, which was wrong — the claim was generalised from the code path without checking its callers, the exact mistake this file records elsewhere. Related to OPEN-ITEMS 39 but distinct: 39 is "the preview differs from the charge"; this is "the supplied value **was** the charge, unverified". Not fixed by Plan H, which audits and does not change behaviour (plan §6). |
| 46 | **A form driven by a server action cannot be submit-tested under vitest** | `package.json` declares `react`/`react-dom` `^18` and `node_modules` resolves **18.3.1**, which has no support for a function-valued `<form action={...}>`. The App Router works because Next.js aliases its own compiled React build at runtime; vitest resolves the plain package and no dispatched submit ever reaches the handler. Found during batch 1 item B (2026-08-19) after five separate attempts to make the event fire — `isTrusted` forcing, native `.click()`, raw listeners — none of which can work, because the capability is absent rather than blocked. **The consequence for `OPEN-ITEMS 38`'s render-test approach:** rendered assertions about markup and state are fine, but *"fill the form, submit, assert the payload"* is not available. The workable seam is to extract the payload-building logic into a pure function and test that directly, plus an action-level test — which is what item B did (`buildConversionSubmission`). Not a defect to fix; a limit to design around, recorded so the next person does not spend the same hour discovering it. Would disappear on a React 19 upgrade (`OPEN-ITEMS 22`, Next.js 14→16). |
| 47 | ~~A consumable's base unit is stored as a name where every other row holds an ID~~ **CLOSED 2026-08-20** | `app/admin/inventory/items/components/PurchasedItemForm.tsx`: `unitOptions` is keyed by unit *name*, so `selectedConsumableBaseUnitId` held `"Cái"` while every consumer treated it as a unit ID (`U-003`). Two symptoms from one mismatch: on create the conversion row rendered the fallback `cơ bản` instead of the chosen unit and submitted `base_unit` as a name into a column whose 57 existing rows all hold IDs; on edit the "Đơn vị gốc" field rendered empty because the stored ID was fed to a name-keyed select. **RAW was unaffected** — there `baseUnitId` comes from `activeBaseIngredient.base_unit`, already an ID. Found 2026-08-20 by the owner testing batch 1 item B on production; visible in his screenshot as `Bao = 10 cơ bản`. **Fixed per `docs/superpowers/plans/2026-08-20-consumable-base-unit-mismatch.md`:** renamed the state to `selectedConsumableBaseUnitName` (it always held what `SearchableSelect` actually emits, a name), derived `baseUnitId` as `units.find(u => u.name === selectedConsumableBaseUnitName)?.id`, and seeded edit mode by converting the stored ID to a name the same way `purchased_unit` already does. Also closed the hole, not just the instance: `buildConversionSubmission` now refuses any `baseUnitId` that does not match a real `units[].id`, on both the RAW and CONSUMABLE paths. **Verified the required render tests fail against the pre-fix code, then pass after** (stashed the component fix, ran both new tests, confirmed `cơ bản` instead of `g` and an empty selector instead of `g`, restored the fix). Zero production `uom_conversions` rows were corrupted by this before the fix landed (checked directly: no row's `base_unit` fails to resolve against `units`); `SPM-053` remains the only CONSUMABLE item, still with zero conversions. Tests 1239 -> 1243. `scripts/verify-revenue.ts`: all four closed months unchanged. |
| 48 | **The standalone "Bảng Quy Đổi" screen cannot save a conversion for a consumable at all** | `app/admin/inventory/conversions/components/ConversionForm.tsx:42-56`: `baseUnit` is derived only through `selectedItem.base_ingredient_id` → `baseIngredients.find(...)` → `units.find(...)`. A CONSUMABLE item has no `base_ingredient_id`, so `baseUnit` is null and the form refuses with the generic `"Vui lòng điền đầy đủ thông tin"` — naming nothing the user can act on, since every field they can see *is* filled. **A different defect from `OPEN-ITEMS 47`**, which was a name/ID mismatch on the Hàng Mua Vào form; this is a missing path. Found 2026-08-20 by Sonnet while implementing 47, verified independently the same day; deliberately not fixed there, since 47's plan scoped itself to one form. **Consequence for Plan J batch 2:** conversions for the 26 consumables must be entered through **Hàng Mua Vào**, not this screen, until it is fixed. The likely fix mirrors 47's: give the consumable path its own base-unit source instead of deriving one it can never have. |
| 49 | **Hàng mua vào has no brand or attribute field, and no named purchase variant** | Owner raised 2026-08-21 before entering the 27 consumables: the create form carries only name, category, ingredient link and conversions. **(a) Brand lives inside the name string** — `Bột cà phê MR.PHIN Robusta Dak Mil`, `Sữa chua không đường Vinamilk`, `Trà xanh lài ngọc lan Kenbar` — so *"how much did I spend on Kenbar this year"* is unanswerable by query. Note this is **not** `brands` (BR-001 Phin Đi / BR-002 Uchako); that is the business brand, carried on `products` and order codes, and the owner explicitly distinguished the two. **(b) A purchase variant has an id but no name:** `uom_conversions` rows are referenced per line by `purchase_order_lines.conversion_id`, so the system does record which pack size was bought, but the row has no name of its own — `Dâu sấy` has **three** ACTIVE conversions all displaying as `Túi` (100 g, 500 g, 1.000 g) and `Kem whipping Anchor` has **two** displaying as `Hộp` (1.000 ml at 182.520đ, 250 ml at 61.560đ), indistinguishable on any ordering screen. **Owner decisions, both 2026-08-21:** the composed-name formula is **tên gốc + thương hiệu + thuộc tính** — verified against real rows, it reproduces `Bột cà phê MR.PHIN Pha Phin Truyền Thống` and `Sữa đặc Ngôi Sao Phương Nam` exactly, losing only punctuation (`- 16OZ`, `, size 90-100mm`); and **the whole item is deferred to the per-feature review pass**, with data entry going first because the information already exists on the products. **Attributes are the expensive half and were flagged as such:** `16OZ`, `size 90-100mm`, `màu đen`, `PET 98` are per-category, so doing them properly means attribute definitions per category, and doing them as free text reproduces the filtering problem it is meant to solve. Related: `OPEN-ITEMS 47`/`48` are the same form's defects; `BR-U-002` parks multi-brand/outlet separately. |
| 50 | **`is_non_inventory` records no effective date, so flipping it would rewrite past P&L** | The flag (`base_ingredients` since earlier, `purchased_items` since migration `0068`, 2026-08-21) is a bare boolean: no `_from`, no `_at`, no history. **Harmless today** — the only live reader for purchased items is stocktake eligibility at session-start (`app/admin/inventory/stocktake/actions.ts`), and nothing computes money from it. **Not harmless in Plan J batch 5:** `BR-COGS-007` makes purchases of flagged items the *"Nguyên liệu mua dùng ngay"* expense line. Computed against the flag's **current** value, un-ticking an item in November silently reclassifies every purchase of it back to March, and closed months' figures move. This is the same objection the owner already made about depreciation on 2026-08-19 — *"nếu anh đổi thời gian khấu hao thì các sản phẩm đã set khấu hao trước đó vẫn giữ nguyên"* — and the same rule `CLAUDE.md` section 8 states: record the value used at the time of calculation, or accept that editing the setting means recomputing everything, and say which. **Proposed, not decided:** stamp each `purchase_order_lines` row at write time with how the item was classified then, so past orders keep their own reading. **Raised by the owner 2026-08-21** asking what happens if he changes his mind after ticking. The *other* half of that question is answered and needs no work: un-ticking loses nothing, and the first count afterwards produces a large variance that is unrecorded consumption, not shrinkage — already covered by `BR-COGS-007`'s zero-issue-slip precondition, and already observed once (the 2026-08-09 first count, 34.864.627đ). |
| 51 | **`Túi đựng khoai` was bought but has no quantity, so it has no home yet** | Owner 2026-08-21, entering batch 2: *"Túi đựng khoai anh sẽ ghi vào chi phí sau, cái này anh không có số lượng cụ thể."* It is the one row of his sheet's 27 consumables deliberately **not** created as a purchased item — with no quantity there is nothing to convert, count, or value, so forcing it into the catalogue would invent a number. It becomes a plain expense line in **Plan J batch 4**. Recorded because it is otherwise invisible: it is absent from the system by design, which looks identical to being forgotten. The sheet shows it at 0 quantity / 0đ today, so nothing is missing from any total yet — the gap appears the first time he buys some and needs somewhere to put the amount. |
| 52 | ~~An EQUIPMENT purchase order line could never be marked COMPLETED~~ **CLOSED 2026-08-22** | `PurchaseOrderForm.tsx` required `conversion_id` on every line unconditionally before completing an order. `Dụng cụ` (EQUIPMENT) items structurally have zero `UOM_Conversions` rows (batch 1 item B section B3 — equipment gets no conversion-rows UI at all, unlike RAW/CONSUMABLE, which the form itself guarantees always carry at least one), so the dropdown had nothing to offer and the requirement could never be satisfied — no equipment purchase could ever be completed. Found 2026-08-22 while critiquing Batch 3's §6 reconciliation, which needs equipment purchases to be enterable at all; the plan's own §1 had assumed "purchasing is the existing flow" without this having been checked. The server side (`lib/purchase-ledger-rebuild.ts`'s `buildPurchaseReceipt`) already handled a missing conversion correctly for any non-RAW item — only the client blocked it. **Fixed the same day**, batch 3: the requirement now applies only when the item has at least one available conversion to choose from (`availableUnitsForLine.length > 0`), extracted as `validatePurchaseOrderLine` and tested directly; RAW/CONSUMABLE items, which always have a conversion, are unaffected. |
| 53 | **Editing a completed purchase order does not touch any `assets` row it already created** | Batch 3's auto-creation hook (`purchase-orders/actions.ts`, on completing a NEW order with an EQUIPMENT line) only fires for `!id` — a brand-new order. What should happen if that order is later **edited** (quantity or price corrected) is not addressed anywhere in the plan: silently re-deriving the asset would risk corrupting `term_months`'s freeze (`BR-COGS-008`) or overwriting a disposal history that may already exist on it. Left unresolved rather than guessed at. Reachable once equipment purchase orders exist and get edited — not reachable today (no equipment purchases entered yet). |
| 54 | **Two different, undocumented-as-such allocation methods compute a purchase line's landed cost** | `lib/purchase-order-cost-allocation.ts`'s `allocatePurchaseOrderCost` is the one `docs/BUSINESS-RULES.md` names as `BR-COGS-006`'s implementation (`lib/issue-costing-inputs.ts` uses it, feeding the COGS report) — it rounds each line's share to whole đồng with a residue correction so shares sum exactly to the adjustment. `lib/purchase-ledger-rebuild.ts`'s `calculateLineLandedCost` (called by `buildPurchaseReceipt`, which writes every `stock_ledger` `PO_RECEIPT` row's `unit_cost`, for every completed purchase order line, RAW and CONSUMABLE alike) is a separate, plain-float proportional allocation with no rounding and no residue correction — never cited by BR-COGS-006 at all. Found 2026-08-22 while deciding which one Batch 3's `assets.unit_cost` should read from (chose `allocatePurchaseOrderCost`, the documented one, via a new `lib/asset-purchase-allocation.ts`, not `calculateLineLandedCost`). **Likely harmless today:** `CLAUDE.md` section 7 states `stock_ledger` no longer drives cost for any report — its `unit_cost` column appears to be written but never read. Not independently confirmed by a full trace of every `stock_ledger.unit_cost` reader; not fixed here, well outside this batch's scope and touches a shared, financially-sensitive file. |
| 55 | **`purchase_order_lines.transaction_date` round-trips through a timezone-ambiguous string** | `purchase-orders/actions.ts`: `effectiveDate = new Date(transaction_date).toISOString()`, where `transaction_date` is sent by the client via `toSaigonIsoString` — a bare `"YYYY-MM-DDTHH:mm:ss"` with **no offset marker**. `new Date()` on a string like that parses it as local time **in whatever timezone the running Node process is in**, not necessarily Saigon. Currently benign in production because Vercel functions run in UTC, which happens to make the math work out for a Saigon-picked date. **Confirmed reachable, not hypothetical:** writing Batch 3's test for the asset-creation hook, the exact same input shifted the resulting date by one day on this machine's own local (non-UTC) timezone — caught only because the test asserted the specific date. Pre-existing throughout `purchase-orders/actions.ts`, not introduced by Batch 3; not fixed here (shared, tested, financially-sensitive file, well outside this batch's scope). |
| 57 | **`getHourlyHeatmapV2` has the identical UTC/local-zone bucketing bug `getSalesDataV2` just had fixed** | `app/admin/reports/actions.ts:772-776`: `d.getDay()`/`d.getHours()` on the raw `new Date(o.created_at)` — the same class of bug `docs/superpowers/plans/2026-08-26-sales-chart-timezone.md` fixed in `getSalesDataV2`'s four chart series, and the same class as `OPEN-ITEMS 55`, now a third site. **Confirmed, not assumed:** correct on this dev machine only because its local timezone happens to be `Asia/Ho_Chi_Minh` (checked directly while proving the sibling fix's test failed first — it only failed under `TZ=UTC`, passed with no override), and wrong on Vercel, which runs UTC. Found while fixing the sibling bug in the same file; deliberately not bundled — the plan scoped the fix to `getSalesDataV2`'s four series only, and a fix spanning both is harder to verify than two that do not (the plan's own reasoning for leaving `OPEN-ITEMS 55` alone too). The fix is the same shape: `saigonBucketKeys` (`lib/report-time.ts`) is already there to reuse. |
| 58 | **The undated-data-claims gate found 16 lines, not the 3 it was budgeted for — running advisory, not blocking, until the scope is decided** | `docs/superpowers/plans/2026-08-26-undated-data-claims.md` section 3 measured its false-positive budget against `CLAUDE.md` alone; section 4 scopes the check (`checkUndatedDataClaims`, `scripts/check-rules-current-core.ts`) to all of `RULE_DOCS`. Run for real: **16 lines, 5.3x the measured budget** — 3 in `CLAUDE.md` (matching the plan's own prediction exactly), and 13 unmeasured because the measurement never opened the other three documents: 5 in `docs/BUSINESS-RULES.md`, 3 in `docs/OPEN-ITEMS.md`, 6 in `docs/operations/orders-v2-cutover.md`. Two real patterns, neither in the original three: **runbook pass/fail thresholds** (`docs/operations/*.md` is full of them — "Stop if drift > 5đ/order" — structurally identical to the already-accepted "~95%" threshold, just never counted), and **a canonical date sitting outside the window in a longer document** (BR-COGS-006's "7,4% wrong" is dated two sections earlier; `docs/OPEN-ITEMS.md`'s Logging Hygiene paragraph states a date once, then a wrapped sentence carries numbers 3-4 lines past a ±2-line window — the plan's own third named false-positive category, just recurring far more in documents with longer paragraphs than `CLAUDE.md`). Per the plan's own instruction for this exact case ("if it is worse than measured, say so and stop rather than shipping a noisy gate people learn to ignore"), none of the 16 were marked with the escape hatch (`<!-- undated-ok -->`) — `scripts/check-rules-current.ts` runs the check and prints every line on every commit, but treats it as advisory (`ADVISORY_CHECKS`) rather than failing the gate, so the scope decision stays live instead of being buried under 16 unreviewed suppressions. **Open: does the check narrow to exclude `docs/operations/*.md` (runbook thresholds are not data claims about current state), or does the accepted budget just widen to reflect the real 16, or something else** — an actual decision, not a default to fall into. |
| 56 | ~~A CONSUMABLE purchase would record stock in purchase units while every count records base units~~ **CLOSED 2026-08-22** | `lib/purchase-ledger-rebuild.ts:61`: `const isRaw = Boolean(input.item.base_ingredient_id); const conversion = isRaw ? resolveConversion(...) : null;` — so for a non-RAW item the conversion was never resolved, `conversionRate` fell to **1**, and `quantity_change` (written to `purchase_order_lines.base_quantity`) was the quantity **in purchase units**. `computeOnHandByPurchasedItem` then subtracts `stock_issues.base_quantity`, which stocktake and issue slips write in **base** units. Concretely: buying `Ống hút nhỏ` as **2 Bao** would have stored `base_quantity = 2`; counting one sealed bag computes **500 g**; on-hand would read **2 − 500 = −498**. `unit_cost` was wrong by the same factor. **`isRaw` was a stale proxy for "has conversions"** — true when written, false since batch 1 gave consumables their own conversions, exactly the shape of `OPEN-ITEMS 47`. **Fixed per `docs/superpowers/plans/2026-08-22-consumable-purchase-base-quantity.md`:** gated on conversion *existence*, not on `base_ingredient_id` and not on conversion `status` — `resolveConversion` already looks up without a status filter (C17 spirit), so an old line pointing at a since-deactivated conversion still resolves rather than silently falling back to rate 1. Second site fixed in the same commit: `lib/historical/purchase-ledger-audit.ts:169` carried the identical stale proxy, discarding an already-resolved conversion's rate for non-RAW items; an audit that disagrees with the path it audits reports false discrepancies. Checked for a third site (broader grep across `lib/` and `app/` for the same `base_ingredient_id ?` shape) — none found. **Verified neutral on all 164 real purchase lines, not by argument:** replayed `buildPurchaseReceipt` before and after the fix, byte-identical output — 164 compared, 0 differed (confirmed 164 is the actual total row count in production, not assumed from the plan). Consumables and equipment still have 0 purchase lines, so nothing in the data was wrong; this closes the gap before the owner enters the first one. Proved the new tests fail against the pre-fix code first (stashed, ran, confirmed the exact wrong values — `quantity_change: 2` not `1000`, matching the plan's own prediction — restored). One pre-existing test's fixture needed a realism fix as a direct consequence of the new gate (it only supplied a conversion for a *different* item, which used to be irrelevant to the old RAW-only gate); not a defect in the fix, but worth naming since a silently-broken test is as bad as a silently-wrong one. |
| 34 | **No way to see an issue slip older than the "recent" list** | `app/admin/inventory/issue-slips` shows the newest 100 `MANUAL` rows (`RECENT_SLIPS_LIMIT`); nothing links to anything before that. Real gap, but a second feature (history/search/pagination), not the D10 layout fix — owner explicitly deferred it out of D10's scope 2026-08-09 rather than have it built there. |
| 36 | **`STK-` prefix names two different id spaces** | Stocktake session ids (`open_stocktake_session_atomic`) and `stock_ledger` row ids (`apply_stocktake_session_atomic`) both use the `STK-` prefix, each numbered from `max()` of its own table — not a collision (different tables, never compared to each other), but two unrelated things wearing the same name. Noted while building D12 (2026-08-09), not fixed there — a rename touches every place either id is displayed or matched by pattern. |

## Chua xac minh duoc tu ben trong

| # | Item | Why open |
|---|---|---|
| 25 | **The bulk-data hook has never been seen to fire** | `.claude/settings.json` gained a `PreToolUse` hook on 2026-08-02 (Task 4). Its command is proven correct by pipe-test, its JSON parses, and both files are tracked by git. What is *not* established is that the harness runs it: a live test from the coordinator's session produced no injected reminder, which does not distinguish a misconfigured hook from a session that cached settings at start. **Owner action:** `/hooks` is unavailable over Remote Control (tried 2026-08-02), so the remaining route is simply a session started after 2026-08-02 — settings load at session start regardless of how the session is opened. In that session, run any command containing `--apply` and say whether the reminder appears. Same for whether `fnbapp-bulk-data-change` shows in the skills listing. |

## Chuong trinh quy tac va cau truc

| # | Item | Why open |
|---|---|---|
| 26 | **Phase 2 — business rules as tests** | Deferred 2026-08-02. It would encode a COGS calculation the owner is about to replace. The display-rounding half does not depend on the calculation and could be written earlier if wanted. Spec: `docs/superpowers/specs/2026-08-01-working-rules-and-repo-structure-design.md`. |
| 27 | **Phase 3 — repository restructure by business domain** | Deferred 2026-08-02, owner sequenced the COGS calculation change first because it lands on `mac-cogs` and `inventory-consumption`, two of the three domain hubs named at the time. Plan C/D have since landed. **Dependency map re-measured 2026-08-10, not resumed** — `docs/audits/2026-08-10-lib-dependency-map.md`: 106 modules now (was 78), and the old "infrastructure not cross-domain" conclusion needed correcting, not copying — `mac-cogs`/`inventory-consumption` are no longer live domain engines (Plan C's cutover moved every screen off them), the real live COGS engine today is `lib/issue-costing.ts`, which the 2026-08-02 map never saw. 51% of `lib/` now has zero live caller at all, mostly one-off historical-correction tooling from Plan C/D. Still not started — this was measurement only. |
| 28 | **Phase 4 — UI/UX rules** | Deferred by owner decision until after the restructure. 28 pages, 15+ inconsistent empty-state patterns measured 2026-07-06. **Gained a standard 2026-08-08:** the owner made mobile-first mandatory for every page including layout (`CLAUDE.md` section 8), so this phase now has a target to converge on rather than only a list of inconsistencies. New and touched pages follow it immediately; the existing 28 are this item's job and are not a licence to rewrite the admin in one pass. **Do not measure this by counting `sm:`/`md:`/`lg:` prefixes.** That count was used on 2026-08-08 and retracted the same day: the reworked `IssueSlipClient` fell from 4 prefixes to 1 while getting better, because `grid-cols-1 lg:grid-cols-2` makes the phone the base case and expands with one rule. Done properly, mobile-first needs fewer prefixes; a high count often marks a desktop layout being undone at each breakpoint. Judge a page by opening it at phone width. **Scope widened 2026-08-17 (owner):** the overhaul covers the **whole system**, and **only the phone version gets built first** — desktop becomes a separate later pass, because phones are the majority of real usage. A page is "done" when it is done on a phone; desktop may stay rough until that pass. This does **not** licence a single sweeping rewrite — the batching rule above still holds, only the finish line moved. Expect data-dense admin screens (reports, purchase-order lists, recipes) to cost *more* than making them responsive, because a phone version of a wide table is a different presentation, not a smaller one. `app/admin/reports/issued` (2026-08-13) is the built example. |
| 33 | **The low-stock warning loses its input permanently, not just its history** | `lib/reorder-suggestion.ts` derives consumption speed from `SALES_CONSUME` and `PRODUCTION_CONSUME`, and feeds `lowStockItems` on the daily dashboard (`app/admin/reports/daily/actions.ts:80`). After Plan B Task 3 nothing writes those row types again, so the feature does not break — it reports "not enough data" forever, silently. It also calls `buildInventoryBalances` from `lib/inventory-consumption.ts`, the file Plan C Task 6 says it removes from the running path, so the plan contradicted itself until the 2026-08-05 challenge round. Consumption speed is still derivable from `stock_issues`, but only as coarsely as counts are taken. **Owner decided 2026-08-05: switch it off with a Vietnamese line on the screen explaining that a count is needed, decide the rebuild later.** The deciding number: `MIN_CONSUMPTION_EVENTS = 3` over a 14-day window (`lib/reorder-suggestion.ts:95-106`), so a weekly count yields two events and every item reports "not enough data" regardless. **Still open:** whether to rebuild once counting frequency is known, and whether to retune the threshold. |
| 32 | **No rule yet for a count that exceeds theoretical stock but not total purchases — RESOLVED, text kept for the record** | Reachable from the **second** stocktake onward, never the first: with no issues recorded, theoretical equals total purchased and the case collapses into `BR-INV-005`. It means an earlier count recorded more as issued than actually left, so cost for that earlier period is overstated and the shelf sits permanently above the books. Plan B Task 3 refuses the line rather than ignoring it, because recording nothing would make the discrepancy recur at every future count with no trace. **Both open questions this row raised are now answered:** the `stock_issues.base_quantity` constraint (`0052`) has accepted a negative value since `0056` (`BR-INV-008`, "found stock"), and whether an over-recorded issue can be reversed was decided as yes, at today's running average (`BR-INV-009`, Plan D D7b, extended 2026-08-09 by D14 to a whole confirmed session). This row is stale, not wrong at the time it was written — kept rather than deleted, matching how this plan marks other superseded sections elsewhere. |
| 31 | **The financial report needs redesigning, and it is not a P&L today** | Owner decision 2026-08-04: discuss after the COGS change lands. Two findings recorded so they are not rediscovered. First, app/admin/reports/pnl/page.tsx (deleted 2026-08-05, Plan C Task 2b) was titled "Báo cáo Lãi Lỗ (P&L)" but stopped at gross profit — there is **no operating-expense table anywhere in the schema**, so rent, wages, utilities and packaging have never been subtracted. July 2026 reads "lãi 11.412.736đ" before any of them. Second, issue-based costing removes the per-drink cost breakdown outright and forces the ingredient-consumption table to be rebuilt around purchased items; Plan C Task 2 carries that forced part. Expense tracking is a separate subsystem and needs its own spec. **Owner decided 2026-08-05 to delete the existing P&L screen rather than maintain it until then** (Plan C Task 2b): revenue lives on `app/admin/reports/sales/page.tsx` independently, and gross profit currently equals revenue exactly because cost is zero, so the page adds no information. `getPnLDataV2` is deliberately kept with no caller — it is the revenue gate for the remaining deletions and the starting point for the rebuild. **Interviewed 2026-08-10 and parked again by the owner — record what was settled so none of it is re-asked.** (a) **Purpose: "Quán tôi có lãi không?"** — so the report is worthless without operating expenses; a revenue-minus-COGS page answers a different question he did not ask. (b) He tracks costs in Excel today but chose to follow a structure designed here rather than have his sheet copied. (c) **No delivery-app sales today, but expected.** When they start, that changes **revenue**, not just expenses — recorded revenue becomes what the platform reports while 20–30% never arrives, so it must not be handled as "one more expense category". (d) Proposed shape: revenue − COGS = gross profit, then rent / utilities / wages / daily consumables / packaging / other = net profit, entered one line per amount with a fixed category list (free-text categories split "Điện" from "Tiền điện" and break the report). **(e) The owner parked it on a gap that proposal missed: equipment.** Buying a 30-million-đồng machine is not a cost of the month it is bought; it is an asset spread over years. Assets and depreciation are a different subsystem from monthly expenses, and the flat category list above cannot carry them. **Resume by designing assets/depreciation and expense entry together, not expenses alone.** |

## Tracking debt

| # | Item | Why open |
|---|---|---|
| 17 | **285 unticked checkboxes across 12 plan files**, most describing finished work | The plans cannot be read to find what is left — the one job they had between sessions. Fix forward: tick as you go, and add a status banner to closed plans. Do not retroactively tick boxes nobody verified. |
| 24 | **`scripts/check-rules-current.ts` covers 3 fixed documents plus `docs/operations/*.md`, not every living document** | Covered as of 2026-08-02: `CLAUDE.md`, `docs/BUSINESS-RULES.md`, `docs/OPEN-ITEMS.md`, and every `.md` under `docs/operations/` (read from disk, so new runbooks are automatic). **Not covered**, so a dead reference in any of these can go unnoticed: `README.md`, `CONTEXT.md`, `ARCHITECTURE.md`, `docs/TESTING.md`, `docs/FEATURE-CATALOG.md`, `docs/ACCESS-MODEL.md`, `docs/FILE-ORGANIZATION.md`, `docs/domain-dictionary.md`. Widening needs a cleanup pass first — several already carry dead links, and the gate would fail on day one. `DEVELOPMENT-TRACKING.md` and the closed `docs/COMPLETED.md` stay out permanently by design; both are chronicles, not living claims about the present. |

## Migrated from ROADMAP.md, now deleted (Task 3b, 2026-08-01)

The old roadmap file is gone as of this task — see `DEVELOPMENT-TRACKING.md`
for the closed-row count. These 6 rows were its only genuinely open items (`[ ]`
or `[~X]`, not folded into a later closed task).

| # | Item | Why open |
|---|---|---|
| 18 | **`INV-COUNT-1` — periodic stocktake: built and live, never once used** | **Corrected 2026-08-02 by checking production; the previous text was wrong on all three counts.** Migrations `0036` and `0037` are both applied (remote confirms `0037`), `apply_stocktake_session_atomic` exists and writes `STOCK_ADJUST` rows, and the UI is complete (`app/admin/inventory/stocktake/**`, `lib/stocktake-transaction.ts`). What is true is that **`stocktake_sessions` holds 0 rows** — the feature has never been exercised. Not stranded work; unproven work. This became load-bearing on 2026-08-02: the owner's new COGS model values goods *issued* from stock, and periodic counting is one of the two ways he records an issue. The stale claim came from copying the old roadmap's row verbatim during Task 3b without verifying it. **Stakes changed 2026-08-07:** Plan C's cutover applied (Task 4/5) before this was ever exercised — see item 15. |
| 20 | ~~**`H1` — 40 local commits not pushed**~~ **CLOSED 2026-08-17** | Pushed on owner approval: `9fcad21..982891f`, **32 commits**, after all four gates. Working tree clean against `origin/main`. Not a standing task — the rule stays that pushing needs the owner's approval each time (`CLAUDE.md` section 2), so this item closes rather than recurring. |
| 21 | **`OPS-CONT-1` — operational continuity audit never run** | Single-owner dependency on the Vercel/Supabase/Google/GitHub accounts: recovery paths, 2FA, what happens if one is lost — never audited in any gate. Needs one session with the owner; output is a plain-Vietnamese runbook kept outside the repo, no secrets stored in it. |
| 22 | **`INFRA-UPGRADE-1` — Next.js 14→16, not started** | Carries `DEP-1`'s remaining `next` advisories, only fixed in `next@16`. Needs owner go-ahead. Full regression bar before merge: `tsc`, full suite, `next build`, P&L/MAC 0-delta audit, live smoke test. |
| 23 | **`V1` — first real operator backdate verify** | Waiting on the operator to backdate a purchase order in the real UI (weekly frequency, per the original user interview). Walk through: list → detail → approve → confirm drift = 0. |

## Future direction (owner priority, set 2026-07-18/19 — sequencing only)

Owner-stated long-term direction, in order. Nothing below starts until the
phase before it is done; do not begin implementation on any of these without
a fresh, explicit go-ahead even after the prior phase closes — this records
intent and order, not authorization to start.

1. **Finish current work** — the eight-gate audit. **Done**: all 8 gates closed
   (see `DEVELOPMENT-TRACKING.md`).
2. **Repository file/folder reorganization** — done 2026-07-20, but that pass
   was docs-and-scripts cleanup only (56 scripts deleted, docs consolidated).
   It is **not** the application-code restructure planned as phase 3 of the
   current working-rules program — that restructure has not started.
3. **Feature-completeness pass** — plan and close gaps so the single-shop
   system fully covers: inventory control, cash in/out control, sales
   reports, order reports, financial reports, and stock reports. Likely
   overlaps with `docs/FEATURE-CATALOG.md` findings and the deferred
   17-section F&B checklist (item 11 above) — reconcile rather than
   duplicate when this phase starts.
4. **UI/UX upgrade and frontend unification** — after feature completeness,
   not before; a consistent UI on top of incomplete features would need
   rework.
5. **Multi-branch management** — first of the two expansion features. Needs
   outlet entity, data isolation, outlet-scoped roles, consolidated
   reporting design (see `docs/FEATURE-CATALOG.md` `ORG-MULTI-OUTLET`). **A
   thin slice landed 2026-08-25** ahead of this phase, on the owner's direct
   instruction rather than as phase 5 starting early: an `outlets` table
   (code + brand link only, not the spec's full `Outlet_Brand_Slot`/
   `Staff_Slot_Assignment` model), the till picks an outlet and the brand
   follows it, and order codes carry the outlet. See
   `docs/superpowers/plans/2026-08-24-outlets-and-order-code.md`. Data
   isolation, outlet-scoped roles, and consolidated reporting design are
   still unbuilt — this phase has not actually started.
6. **Full permissions and security hardening** — done once the system's
   shape through multi-branch is known, to avoid designing the permission
   model twice. Distinct from the eight closed security gates, which stay
   scoped to the current single-shop system; this phase is the full
   `docs/ACCESS-MODEL.md` Phase 3 verification plus whatever multi-branch
   roles add.
7. **Franchise management** — moved to last (owner decision 2026-07-19): not
   yet certain this gets built at all, so nothing before it should be
   designed around it, including the security-hardening phase. If approved
   later, its tenant-isolation needs get their own follow-up security
   review at that time.

## Out of scope (do not start without explicit approval)

- **Negative stock recovery** — needs a physical count decision from the
  owner. Figures here go stale fast; the current state is whatever the most
  recent rebuild audit says, not a number copied into this file. Most recent:
  `docs/audits/2026-07-29-phase4-rebuild-dryrun.json` /
  `-apply.json` (Phase 4 full-history rebuild) — as of that pass, the only
  remaining negative was Muối hồng, -14.39 g, root-caused to a purchase never
  entered under its correct item mapping.
- **Franchise system** — see "Future direction" above; comes after
  multi-branch, needs design + business rules (multi-tenant RLS, franchisee
  role, outlet management).
- **Multi-branch system** — see "Future direction" above; comes after the
  feature-completeness pass and UI/UX unification, needs design + business
  rules (outlet entity, data isolation, outlet-scoped roles).
- **Historical data rewrite** — any rewrite of pre-2026-07 data requires
  explicit owner approval, dry-run, atomic transaction, and verification.
- **Auth system overhaul** — the placeholder "admin" reviewer in the backdate
  UI is a known gap, but full auth is separate scope; see "Future direction"
  item 6, deliberately last.
- **17-section F&B capability checklist** — deferred from the pre-audit
  phase; needs owner per-item priority classification when scheduled.
